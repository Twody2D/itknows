# Performance Budget

Обновляется после каждой фазы (`pnpm build && pnpm size`). Числа ниже —
фактические измерения, а не оценки.

## Целевые показатели

| Метрика | Цель | Жёсткий потолок ТЗ |
|---|---|---|
| FPS на слабом мобильном устройстве | стабильные 60 | — |
| Размер `dist/` | < 3 МБ | 10–20 МБ (§3 ТЗ) |
| Аллокации в hot path (`update()`) | 0 | — |
| Частицы одновременно на экране | ≤ 150 (авто-снижение на слабых устройствах) | — |

## Стратегия ассетов

- **Текстуры**: генерируются процедурно на offscreen `<canvas>` в `src/art/`
  и регистрируются как Phaser-текстуры в рантайме. Ни одного PNG/JPG в
  репозитории или в билде.
- **Аудио**: синтез через Web Audio (Phase 5). Ни одного MP3/OGG в билде.
- **Шрифты**: bitmap-шрифт из glyph-данных (Phase 4) для игровых надписей
  плюс две self-hosted гарнитуры интерфейса — JetBrains Mono и Rubik,
  ~300 КБ `.woff`/`.woff2` в билде. Это осознанное исключение из «ноль
  бинарных файлов», разрешённое владельцем при редизайне меню и подробно
  обоснованное в `src/ui/fonts.ts`; строка «ни одного `.ttf`/`.woff` в
  билде» стояла здесь и после того, как исключение уже было принято, —
  документ расходился с фактом. Файлы отдаются с собственного origin
  (`@fontsource` — dev-зависимость, Vite кладёт файлы в `dist/`), поэтому
  внешних запросов по-прежнему ноль и игра работает офлайн и внутри
  iframe Яндекса.
- **Иконки**: инлайн-SVG там, где нужны (например, favicon), без сторонних
  icon-паков.

## Стратегия зависимостей

Единственная runtime-зависимость — `phaser`. Она же — основной вклад в
размер бандла (минифицированный Phaser ≈ 1.4 МБ), и это ожидаемо: весь
игровой код проекта на его фоне пока измеряется килобайтами.

## Замеры по фазам

| Фаза | Дата | `dist/` всего | Крупнейший файл | Комментарий |
|---|---|---|---|---|
| Phase 0 + vertical slice | 2026-09-03 | 1.43 МБ | `phaser-*.js` — 1.41 МБ | Один сектор (3 уровня), меню, игрок, базовые тайлы. Ноль ассетов сверх Phaser. |
| Phase 1 завершена | 2026-09-03 | 1.43 МБ | `phaser-*.js` — 1.41 МБ | Все 6 уровней сектора 01, односторонние платформы, dev-only debug-хук (вырезается из прод-сборки). Рост своего кода — доли КБ. |
| Phase 2 + Phase 3 завершены | 2026-09-03 | 1.46 МБ | `phaser-*.js` — 1.41 МБ | Полная система ловушек (13 типов) + THE SYSTEM (BehaviorTracker/PlayerProfile/SystemMemory/DifficultyDirector/Commentator) + 9 уровней с 2 демонстрационными вариациями + ~40 реплик RU/EN. Свой код (`index-*.js`) вырос до ~45 КБ — весь прирост от текста/логики, не ассетов. |
| Сектор 02 завершён (12/30 уровней) | 2026-09-03 | 1.46 МБ | `phaser-*.js` — 1.41 МБ | +3 уровня (FALSE FLOOR/SHORT CIRCUIT/SECTOR EXIT) — все 12 типов динамических ловушек теперь задействованы хотя бы в одном уровне. Своя часть бандла — ~46 КБ, рост на уровень измеряется сотнями байт (данные, не ассеты). |
| Сектор 03 завершён (18/30 уровней) | 2026-09-03 | 1.46 МБ | `phaser-*.js` — 1.41 МБ | +6 уровней (PISTON ROW/OFFSET/CATWALK/DELAY LINE/PRESSURE VALVE/CORE ACCESS) — INDUSTRIAL CORE, лазеры. Своя часть бандла — ~48 КБ. |
| Сектор 04 завершён (24/30 уровней) | 2026-09-03 | 1.46 МБ | `phaser-*.js` — 1.41 МБ | +6 уровней (SYNC GATE/RHYTHM/CRUMBLE RUN/FREEFALL/LOCKSTEP/ARCHIVE CORE) — DATA DISTRICT, тайминг-ловушки. Своя часть бандла — ~50 КБ. |
| Сектор 05 завершён (30/30 уровней — кампания полностью) | 2026-09-03 | 1.47 МБ | `phaser-*.js` — 1.41 МБ | +6 уровней (PATROL/CROSSFIRE/OVERDRIVE/GRIDLOCK/LAST GATE/SYSTEM CORE) — финал, комбинация механик секторов 02-04 плюс первое применение `moving-spike` в контенте. Своя часть бандла — ~53 КБ. |
| Реплики SYSTEM (91) + прогрессия личности | 2026-09-03 | 1.47 МБ | `phaser-*.js` — 1.41 МБ | Реплики ~40 → 91 (8 категорий × RU/EN), anti-repeat переписан на per-category shuffle bag. `SystemPersonality.personalityTag()` подключён к HUD-префиксу. Своя часть бандла — ~58 КБ. |
| Арт-ресет (визуальный slice-гейт) | 2026-09-03 | 1.48 МБ | `phaser-*.js` — 1.41 МБ | Собственный bitmap-шрифт (`art/font`, EN+RU) заменил системный monospace везде; персонаж, тайлы земли/платформ и выход перерисованы (модульные материалы, честный маяк выхода); добавлены процедурные parallax-слои (`art/Environment.ts`); меню собрано как композиция (лого + подчёркивание + SYSTEM-марker + PLAY-панель с hover/press) вместо голого текста на пустом canvas. DOM-оверлей для текста (введённый для читаемости) удалён — bitmap-шрифт сам по себе чёткий под `pixelArt`-апскейлом, DOM-слой больше не нужен. Своя часть бандла — ~70 КБ. |
| Hard-reset игрового экрана (композиция Level 01) | 2026-09-03 | 1.49 МБ | `phaser-*.js` — 1.41 МБ | `art/hash.ts` — детерминированный хеш для декоративной нерегулярности (без RNG в геймплее). `Environment.ts` переписан: неравномерная застройка far/mid вместо `col % step`, доминирующая «вышка» как фокус-точка композиции, сенсор-узел THE SYSTEM (кольцо + медленный луч на transform-tween, без per-frame редро) виден только там, где нет своей композиции переднего плана (`focalPoint` флаг — выключен на главном меню, иначе вышка перекрывала лого/персонажа), провисающие кабели вместо гребёнки прямых линий, редкие лит-окна вместо сетки сигналов, мягкая многослойная дымка вместо чёрной пустоты неба. `drawTiles.ts` — 4 варианта шва верхней панели земли + редкий «повреждённый» тайл, выбор по хешу от `(levelId, col)` вместо `col % 6`; платформы получили редкий болтовой вариант. Выход получил SYSTEM-руну на притолоке и `postFX`-glow. Найден и исправлен реальный баг: `GameplayScene` передавал в окружение `worldHeight` (270, включая зону падения под полом) вместо видимой линии пола (`groundRow`), из-за чего здания были частично скрыты под землёй — тот же класс бага, что уже чинили в `MainMenuScene`. Своя часть бандла — ~74 КБ. |

| FX-система (частицы/warning-pulse/shake) | 2026-09-03 | 1.49 МБ | `phaser-*.js` — 1.41 МБ | `fx/FxManager.ts` — пул из 4 частично-статических `ParticleEmitter` (пыль прыжка/приземления, смерть, победа; каждый создан один раз в конструкторе, дальше только `.explode()`, ноль аллокаций в `update()`), плюс warning-pulse (быстрый alpha-tween на самом hazard-объекте трапа, стартует на `trap:armed`/стопается на `trap:triggered`, найден и заведён id-lookup через новое поле `LethalHazard.visual` — у electric-floor видимая поверхность отдельна от hitbox-зоны, ловушка стала бы пульсировать невидимый объект без этого поля), `shake()` на камеру с тумблером `FxSettings.shakeEnabled` под будущий Settings-экран. Смерть/победа получили flash + pixel-глитч-срезы (без шейдеров) в пределах тайминг-бюджета CLAUDE.md #5. **Найден и исправлен баг в процессе проверки**: пыль прыжка/приземления изначально ничего не показывала — частицы рождались ровно на цвет `cyanDim`, совпадающий с cyan-кромкой земли (тот же токен палитры), и маскировались; исправлено смещением точки спавна на 3px выше поверхности и более контрастными цветами (`cyan`/`white`). Проверено скриптовой инспекцией живых частиц/tween-состояния через Playwright (не только скриншотом — короткие эффекты не всегда попадают в кадр). Своя часть бандла — ~78 КБ. |
| Phase 4 закрыта (UI-полировка + реальная длина уровней + fade-переходы) | 2026-09-04 | 1.53 МБ | `phaser-*.js` — 1.41 МБ | Третий проход шрифта (мягче: срезанные углы, одна тень вместо чёрного кольца обводки). `PixelButton` пересобран — размер по тексту вместо фиксированной коробки, скошенные углы, скобки, подчёркивание на hover. `ScaleController` — дробный масштаб с квадратными пикселями вместо целочисленного зума с растяжением холста (убрана чёрная полоса от дисторсии). Секторы 01–02 переписаны с 40–70 тайлов на 120–250 (реальная длина вместо только метаданных секций), `Level.ts` собирает землю прогонами, а не по колонке; секторы 03–05 расширены до 64 тайлов (санити-тест «уровень ≥ MAX_VIRTUAL_WIDTH», убирает вторую причину чёрной полосы). Чекпоинты прорежены до 0–2 на уровень. Мобильное управление — плавающий джойстик + press-anywhere зона прыжка вместо трёх кружков на фиксированных местах (`main.ts`: `activePointers: 2`); найден и исправлен баг нулевой hit-area у `Zone.setInteractive()`. Отдельная UI-камера в `GameplayScene` — на тач-устройствах мир зумится (макс ×1.22), HUD/кнопки — нет. Обучение показывает клавиши-капсы (или жесты на телефоне) вместо текстовой строки, не пропадает мгновенно. Процедурный parallax-фон стал разным по секторам (`SectorTheme`). Fade-переходы между сценами — только на осознанной навигации, никогда на death-retry. Попутные багфиксы: убран Shift-рывок, починен краш при падении в пропасть (сработавший после удаления рывка), «Заново» в паузе теперь сбрасывает счётчик смертей. Своя часть бандла — ~116 КБ. |
| Магазин — вертикальный срез (`docs/SHOP.md`) | 2026-09-05 | 1.57 МБ | `phaser-*.js` — 1.41 МБ | CREDITS-экономика, `CurrencyService`/`InventoryService`/`PurchaseManager`, каталог (3 скина + 2 Death FX + 2 SYSTEM-пака, каждый с одним бандл-эксклюзивом + `remove_ads`/`system_access`), `YandexGamesService` — facade платежей, `ShopScene` (SYSTEM ARCHIVE) как оверлей, dev-режим (`window.__shopDev`, подтверждено grep'ом на ноль вхождений в прод-сборке). Своя часть бандла выросла с ~116 КБ до ~162 КБ — весь прирост от логики/данных/UI, ни одного нового ассета. |

| Редизайн главного меню (макет 1a «витрина + командный столб») | 2026-09-05 | 1.59 МБ | `phaser-*.js` — 1.41 МБ | Меню пересобрано по внешнему макету: панель-витрина с надетым скином игрока, пьедестал и конус света, счётчик CREDITS, крупная кнопка ИГРАТЬ и сетка 2×2 с иконками вместо колонки из пяти равных текстовых кнопок. Новые `ui/MenuTile.ts` (лицо на «подошве», hover/press, расширенная тач-зона) и `ui/MenuIcons.ts` (шесть пиктограмм чистой геометрией — ноль новых ассетов). Раскладка вынесена в `config/menuLayout.ts` и закрыта `tests/menu-layout.test.ts` (safe-зона 480px, отсутствие пересечений, свип по всему диапазону 480–620). Гибридная типографика: bitmap для латиницы/техно-токенов, системный sans через `DomTextOverlay` для русских подписей — с явным скрытием DOM-слоя под оверлеями (canvas-затемнение DOM-текст не перекрывает). Фон меню — процедурный радиальный градиент на canvas-текстуре + сетка, `buildEnvironmentLayers` в меню больше не используется (остаётся в геймплее). Версия в SYSTEM-строке берётся из `package.json` через `__APP_VERSION__`. Своя часть бандла — ~178 КБ (было ~162 КБ). Бинарных ассетов в `dist/` по-прежнему ноль (проверено `find`). |

| 5 новых типов ловушек (`spike-bank`/`spike-wall`/`orbit-spike`/`swinging-spike`/`loop-spike`) + первое содержимое | 2026-09-05 | 1.59 МБ | `phaser-*.js` — 1.41 МБ | Расширение за пределы мастер-промпта §14 (13 → 18 типов, зафиксировано в TODO.md), по прямому запросу владельца. Две таймингованные (`spike-bank` — повторяемый, честно телеграфируемый банк шипов, полоса из `width` тайлов; `spike-wall` — летальная версия `timing-gate`, растёт по ширине от фиксированного края, а не по высоте) и три непрерывно-движущиеся (`orbit-spike` — постоянное вращение; `swinging-spike` — маятник с затуханием на краях дуги; `loop-spike` — патруль по замкнутому многоугольнику в одну сторону, `scene.add.tweenchain`). Пять bold-вариаций (`sector-02-level-02/04/05`, `sector-01-level-04/05`) — таймингованные типы легли на обязательный путь с тем же запасом по дистанции, что и ambush-шипы; непрерывно-движущиеся — только на опциональные бонус-платформы, тем же принципом, что исторически ограничивал обычный `moving-spike` до появления его ambush-варианта. Подтверждено вживую headless-браузером: честный цикл idle→warning→active→cooldown с `isLethal()` строго только в `active` для таймингованных типов; непрерывное видимое движение для всех трёх остальных; ноль ошибок консоли на всех 5 уровнях. Своя часть бандла — ~184 КБ (было ~178 КБ). |

| Gentle-пара для пяти новых типов ловушек + gentle/bold для GAP AND SPIKE | 2026-09-05 | 1.59 МБ | `phaser-*.js` — 1.41 МБ | Каждый из пяти новых типов получил `gentle`-контрпару к своему `bold` (дольше warning/медленнее движение против жёстче/быстрее). `sector-01-level-03` (GAP AND SPIKE, ранее только `troll`) получил полноценные `gentle`/`bold` через собственные ambush-шипы и sudden pits: `warningMs` и дистанция триггера до колонки приземления масштабируются вместе, чтобы честность («бежишь не реагируя — попался, останавливаешься на предупреждении — жив») сохранялась на любой настройке — не единая пропорция для обоих шипов уровня, а под фактически доступное чистое пространство у каждого (7 тайлов у одного, 6 у другого — иначе `gentle` уводил триггер прямо на статичный шип-кластер). Тест `difficulty-director.test.ts` расцепили от реального контента (`vi.mock` синтетического `LEVEL_VARIANTS`) — раньше он был завязан на то, что у `sector-01-level-03` нет `gentle`/`bold`, и это перестало быть правдой. Подтверждено вживую headless-браузером через реальный цикл ввода: `unreacting` умирает точно в фазе `active` на колонке приземления для gentle и bold; `reacts` (отпускает вправо на `warning`) переживает оба варианта. Своя часть бандла — ~187 КБ. |

| `LeaderboardService` (Phase 6): гостевое чтение, авторизация по явному действию, счёт только по каноничной вариации | 2026-09-05 | 1.60 МБ | `phaser-*.js` — 1.41 МБ | `YandexGamesService.ts` получил `leaderboards`/`auth` части фасада (`submitScore`/`getLeaderboardEntries`/`getPlayerLeaderboardEntry`/`requestAuthorization`/`isPlayerAuthorized`) той же деградацией, что и весь остальной фасад. `src/services/LeaderboardService.ts` — игровое правило поверх фасада: счёт уходит только для `'standard'`-варианта (адаптивные не сравнимы между игроками), имя таблицы `level-${levelId}`. `src/services/LeaderboardSubmission.ts` — самоподключающийся модуль на `level:completed`, тот же паттерн, что `shop/EconomyRewards.ts`. Новая строка «Yandex ID» в `SettingsScene` — единственная дверь в `requestAuthorization()` во всей игре, рендерится только когда SDK доступен. **Реальный, задокументированный пробел**: сами таблицы лидербордов должны быть заранее созданы в консоли Yandex Games по имени — из клиентского кода это не автоматизируется. Подтверждено вживую headless-браузером: вне SDK строка не рендерится и `level:completed` не кидает ошибок; отдельно, с фейковым SDK внутри настоящего вложенного iframe (тот же приём, что раньше для `AdsService`/`PurchaseManager`) — гость не отправляет счёт, реальный клик по кнопке проводит через `openAuthDialog`, каноничное завершение отправляет округлённое время под верным именем, `bold`-завершение того же уровня счёт не шлёт, чтение чужой/своей записи работает. Своя часть бандла — ~189 КБ (было ~187 КБ). |

| Daily Challenge — MVP-архитектура (Phase 6, §73): детерминированный уровень дня + серверное время | 2026-09-05 | 1.60 МБ | `phaser-*.js` — 1.41 МБ | `src/gameplay/DailyChallenge.ts` — `getDailyChallenge()` хеширует UTC-дату тем же FNV-1a, что уже используется для декоративной геометрии (`art/hash.ts`), и берёт уровень кампании по остатку от деления — один день даёт один и тот же уровень для всех игроков. `YandexGamesService.getServerTime()` (`ysdk.serverTime()`) используется когда доступен, иначе `Date.now()`. Играется всегда в `'standard'`-варианте: `GameplayScene` получила `forceVariantId`, полностью обходящий `DifficultyDirector`. Точка входа — кнопка «Испытание дня» в `LevelSelectScene`. Осознанно отложено на следующий инкремент (§74): экран результата, rewarded-continue, отдельная таблица лидерборда для Daily Challenge. Подтверждено вживую headless-браузером на обоих путях (без SDK и с фейковым `serverTime()` внутри настоящего iframe), ноль ошибок консоли. Своя часть бандла — ~190 КБ (было ~189 КБ). |

| Ghost-система (Phase 6, §40) + фикс клик-through в оверлеях меню/Sector Complete | 2026-09-06 | 1.60 МБ | `phaser-*.js` — 1.41 МБ | `GhostRecorder`/`GhostSprite`/`GhostService`/`GhostSettings` — запись и воспроизведение личного лучшего забега, плоский `[t,x,y,facing]×N`-сэмпл (~8Гц, потолок 500 сэмплов), привязка к `${levelId}::${variantId}`. `SaveService` v2 → v3 (поле `ghosts`, мёрдж «побеждает быстрейшее время»). Настройка в `SettingsScene` — персистентный тумблер той же формы, что звук/частицы/тряска. **Найден и исправлен реальный баг**, всплывший при живой проверке записи: `GameplayScene.attemptStartMs` читался как `0` на самом первом уровне сессии (собственные часы только что созданной сцены ещё не тикали к моменту `create()`) — искажало `attemptElapsedMs` для `Commentator`, а с этой правкой сломало бы и первый ghost-сэмпл. Исправлено ленивым захватом на первом тике `update()`. **Отдельно** владелец руками поймал баг из редизайна меню: тёмная подложка Shop/Settings/How to Play/Sector Complete-оверлея не перехватывала ввод, так что клик по экрану там, где под панелью стоит ИГРАТЬ/СЛЕДУЮЩИЙ, реально запускал уровень сквозь открытый оверлей — исправлено (`input.enabled = false` на сцене снизу на время оверлея, восстановление по `SHUTDOWN`), подтверждено вживую по всем четырём точкам входа реальными кликами по экранным координатам. Своя часть бандла — ~194 КБ (было ~190 КБ). |

| Ghost по умолчанию выключен + 5 новых ловушек перенесены в base-геометрию (обе правки — прямой запрос владельца, 2026-09-06) | 2026-09-06 | 1.60 МБ | `phaser-*.js` — 1.41 МБ | Живая проба: непрошеный второй персонаж рядом с игроком отвлекает, а не помогает — `GhostSettings` дефолт `enabled: false` (запись остаётся безусловной, тумблер в Settings включает показ в любой момент без потери уже накопленных рекордов). Отдельно: `spike-bank`/`spike-wall`/`orbit-spike`/`swinging-spike`/`loop-spike` были заперты только в `bold` пяти уровней, который `DifficultyDirector` отдаёт лишь после 3 чистых уровней подряд — обычное прохождение их не показывало вообще. Перенесены в BASE-определение (`sector01.ts`/`sector02.ts`) со стандартным для кампании таймингом — присутствуют в любом варианте; `gentle`/`bold` в `variants.ts` теперь только перетюнивают уже существующую ловушку, а не добавляют дубликат. `level-validator.test.ts` (честность/проходимость всех уровней × всех вариантов) остался зелёным без изменений. Своя часть бандла — ~194 КБ (без изменений, чистая перестановка данных). |

| Косметика раунд 2: 2 скина + 4 трейла (по макету Claude Design) | 2026-09-06 | 1.61 МБ | `phaser-*.js` — 1.41 МБ | `patrol`/`echo` скины (`SKIN_VISUALS`, новые именованные токены в `palette.ts`). Новая категория «трейлы» — `SaveService` v3→v4 (`ownedTrails`/`equippedTrail`), пятая вкладка в `ShopScene`. `src/gameplay/TrailFx.ts` — 4 предмета (`data_trail`/`launch`/`interference`/`beep7`-дрон), собственные фиксированные пулы `Rectangle`-частиц вместо `Phaser.ParticleEmitter` (нужны per-spawn значения — текущая скорость игрока, случайный цвет — которые декларативный конфиг эмиттера не выражает), ноль аллокаций в `update()`. Подтверждено вживую: цвета/размеры/пороги каждого трейла побитово совпадают с макетом, дрон сходится к цели и гаснет/зажигается по таймеру, вкладка «ТРЕЙЛЫ» открывается реальным кликом. Своя часть бандла — ~201 КБ (было ~194 КБ). |

| Экран результата сектора (макет 2c) + честные best-times по сектору | 2026-09-06 | 1.61 МБ | `phaser-*.js` — 1.41 МБ | `SectorCompleteScene` пересобран под внешний макет: банер/TIME/DEATHS/BEST/лидерборд/кредиты вместо общего `drawPanel`-оверлея. `SaveService` v4→v5 (`sectorBests`), `LeaderboardService` — секторные `submitSectorScore`/`getSectorEntries`/`getPlayerSectorEntry` (без ограничения на каноничный вариант — у секторного времени в принципе нет единого «каноничного» прогона). Радиальный фон меню вынесен в переиспользуемый `art/ProceduralBackdrop.ts`. Дважды найден и исправлен реальный баг при живой проверке: подпись кнопки ДАЛЬШЕ создавалась раньше самой `MenuTile` и оказывалась под непрозрачной заливкой (canvas-объекты рисуются в порядке добавления — в отличие от DOM-слоя меню, который от этого не зависит); подписи TIME/BEST дублировались на двух языках сразу («ВРЕМЯ · TIME») при уже существующей смене языка в настройках — убрано. Своя часть бандла — ~206 КБ (было ~201 КБ). |
| Компактный HUD в игре (макет 4h) + честная формулировка про ловушки | 2026-09-06 | 1.62 МБ | `phaser-*.js` — 1.41 МБ | Старая 168×56-панель с красным «DEATHS N» (читается как «ты уже проиграл») заменена одной плашкой 8,8 (время + счётчик попыток оранжевым с нейтральной иконкой вместо слова), название уровня ушло в мелкую подпись под ней; добавлена полоса прогресса по уровню (реальные данные — `player.x / level.worldWidth`, та же дробь, что уже использует `Commentator.commentOnDeath`) и подложка-плашка под репликой SYSTEM. Осознанно не перенесён счётчик «чипов» из макета — собираемых предметов внутри уровня в игре не существует, это была бы новая механика, а не редизайн (решено с владельцем). Живой таймер отображается целыми секундами, а не десятыми долями макета: `PixelLabel.setPixelText` каждый раз пересобирает canvas-текстуру, и делать это 60 раз/сек нарушило бы «ноль аллокаций в update()» (CLAUDE.md #9) — обновление раз в секунду держит тот же бюджет, что и остальной HUD. В «Как играть» реплика про ловушки заменена с «ловушки всегда предупреждают» на «осторожно — повсюду ловушки» (правка владельца). Подтверждено вживую: счётчик попыток и полоса прогресса переживают смерть/рестарт без утечек графики, SYSTEM-плашка меняет размер под текст. Своя часть бандла — ~208 КБ (было ~206 КБ). |

| Полный редизайн магазина — витрина на все 5 категорий (макеты 3a/3b/4a/4b/4c из Claude Design) | 2026-09-06 | 1.64 МБ | `phaser-*.js` — 1.41 МБ | `ShopScene` переписана целиком: вертикальный рейл категорий + сетка карточек с живым превью + фиксированная панель «ПРИМЕРКА» вместо старых горизонтальных вкладок и сетки из одних текстовых плашек — теперь у ВСЕХ пяти категорий (ОБЛИК/ТРЕЙЛЫ/ФИНАЛ/SYSTEM/БЕЗ РЕК.), не только у персонажа. Категория БЕЗ РЕК. переставлена с первого места в конец рейла (золотой ромб) — по новому макету, отменяет более раннее решение «premium всегда первым». Превью честные и живые, не статичные картинки из макета: ТРЕЙЛЫ гоняют настоящий `TrailFx` на фейковом колеблющемся состоянии игрока; ФИНАЛ крутит настоящий `FxManager.deathBurst` раз в 1.5 с на самом спрайте; SYSTEM показывает реальные сэмплированные реплики (`general`/`fall`/`successful_adaptation`) из настоящих пулов диалогов — для `corrupted` (эксклюзив бандла SYSTEM ACCESS, ещё не подключён к `Commentator`) честно показывается «тон ещё не настроен» вместо выдуманных строк. Новый скин `core` («ЯДРО») — реальное условие разблокировки `SaveService.getCompletedLevels().length >= getAllLevels().length`, с автовыдачей в инвентарь в момент выполнения условия (иначе получалась мёртвая кнопка «КУПИТЬ» без цены). `FxManager.deathBurst` получил настоящий третий вариант `data_wipe` (раньше бандл выдавал его, но эффект ничем не отличался от `static` — обнаружено при подготовке живого превью, исправлено заодно). Оффер «БЕЗ РЕКЛАМЫ»: убраны «Спроси взрослого» и кнопка «Восстановить покупку» (уже вызывается автоматически при старте) по прямой правке владельца, кнопка КУПИТЬ центрируется по освободившейся высоте; добавлено реальное разовое начисление +500 CREDITS при покупке `remove_ads` (решение владельца — раз оффер обещает подарок, он должен быть настоящим). `DomTextOverlay` — новая опция `clampLines` (CSS line-clamp) вместо ручного обрезания строк по количеству символов. **Три реальных бага найдены и исправлены живой проверкой Playwright**: (1) `railHandles` не сбрасывался между повторными `scene.launch('ShopScene')` — таких вызовов много за сессию (клик по плитке «Магазин» в меню каждый раз) — второе открытие магазина падало на `disableInteractive()` уже уничтоженной зоны; (2) два DOM-лейбла в правой колонке создавались без добавления в список на удаление и утекали при каждой смене категории; (3) сетка категории ОБЛИК при владении бандлом SYSTEM ACCESS вырастает до 7 карточек — фиксированная сетка 3×2 продавливала третью строку за нижний край фиксированного 270px-канваса; высота карточки теперь считается от количества строк. Своя часть бандла — ~228 КБ (было ~208 КБ). |

| Магазин: правки после разбора макета владельцем | 2026-09-10 | 1.64 МБ | `phaser-*.js` — 1.41 МБ | Разбор по замечаниям: (1) кнопка КУПИТЬ/НАДЕТ пересобрана по макету — золотой градиент с белой рамкой и монетой для денег, тёмная плашка с цветной рамкой, галочкой и белым текстом для экипировки; «странная тень» была подошвой на всю высоту кнопки, теперь это 4px-полоса, исчезающая при нажатии (кнопка «садится» на неё). Надписи стали терсовыми («Надеть»/«Надет» вместо «Экипировать»/«Экипировано») — иначе слово занимало кнопку целиком. (2) Превью трейла: настоящая анимация бега (`player-run`/`jump`/`fall`) на полу вместо «летающего» спрайта; бег идёт на реальной `PHYSICS.moveSpeed`, потому что именно она задаёт, как ложатся частицы. Пол поднят внутрь сцены: выхлоп ЗАПУСКА бьёт вниз (это тяга, как и в игре) и у самого низа кадра был не виден, а полосы ПОМЕХИ рисуются на высоте головы и требуют места сверху. (3) Блок «БЕЗ РЕКЛАМЫ» пересобран: золотой баннер с ромбом внутри карточки, три галочки и строка-примечание с пустым чекбоксом, как в макете; тексты пунктов написаны под то, что сборка реально делает (ролики бывают только на стыке секторов, подарок 500 монет — настоящее начисление), обещания «мгновенного респавна» нет: бюджет смерть→рестарт одинаков для всех (CLAUDE.md #5). (4) Реплики SYSTEM больше не пустуют — добавлены реальные строки на каждую категорию в `data/dialogues/shop.ts`, панель держит последнюю реплику, а не гасит её по таймеру. (5) Описания предметов переписаны под слот 126×22 (две строки, ~44 символа) — раньше обрывались многоточием; превью пака SYSTEM берёт три самые короткие настоящие реплики, чтобы каждая влезала целиком. (6) Карточки: цена рисуется монетой с числом, у каждой категории своя мини-сцена (три эффекта ФИНАЛА больше не выглядят одинаково, четыре трейла тоже), флаг «ТОП» у премиум-редкости, затемнение спрайта, когда не хватает монет. **Найденный при этом реальный баг**: шторка, прикрывавшая вылет частиц, закрашивала половину панели ФИНАЛА (кнопку и левый кадр) — заменена на геометрическую маску прямо на `FxManager` (`setClipMask`), плюс `deathBurst` получил флаг `shake`, чтобы превью не трясло весь экран магазина каждые 1.6 с. Своя часть бандла — ~236 КБ (было ~228 КБ). |

| Второй разбор макета владельцем: магазин + окно кредитов, «Как играть» и настройки (4d/4f/4g) | 2026-09-10 | 1.66 МБ | `phaser-*.js` — 1.41 МБ | **Магазин.** (1) Реплики SYSTEM пропадали целиком на канвасе уже 480px: правая колонка живёт от x=492, и на «безопасной» ширине её просто нет — теперь при нехватке места реплика уезжает в полосу под сеткой (в макете там точки страниц, которых у этого магазина нет), а высота карточек считается с учётом этой полосы. (2) Названия и цены на карточках стояли ниже макета: высота превью бралась как `h - 28`, из-за чего арт растягивался с карточкой; теперь арт фиксированные 60px, а плашка забирает остаток и центрирует пару «имя + цена» по себе. (3) Галочка «надето» перерисована по макету — не штрих полилинией (митра в углу плюс сглаженные концы читались кляксой на пиксельной поверхности), а «уголок» из двух 2px-полос, повёрнутый на -45°; сам бейдж выровнен по правому краю карточки, а не торчит за него. (4) Текст на кнопке КУПИТЬ сделан белым: макет специфицирует `#05050A`, но тёмное на золотом при этом кегле читалось как дыра в лицевой панели, а не как слово (правка владельца после живого просмотра). (5) Описание налезало на строку «ОСТАНЕТСЯ N»: координаты из макета брались буквально, а DOM-текст 9px рендерится 11.7px в строку (line-height 1.3), то есть две строки занимают 23.4px вместо заявленных 22 — ритм панели пересчитан от реальной высоты (имя 132, описание 146, статус 180, кнопка 196). (6) Площадка «ПРОБНОГО ЗАБЕГА»: пол поднят внутрь кадра ради выхлопа ЗАПУСКА, и всё под ним теперь закрашено как настоящая платформа, а не лёгким тоном — иначе бегун читался висящим над пустой полосой. **Окно кредитов (4d)** переписано с трёх текстовых строк на макетную раскладку: бесплатная половина сверху, платная снизу. Осознанное отличие от макета: карточек «ВХОД В ИГРУ» (серия ежедневных заходов) и «ЗАДАНИЯ» в сборке нет ни в каком виде — вместо них перечислены реальные источники из `EARN_AMOUNTS` (ролик +20, уровень +10 и +10 без смертей, сектор +25), а полоса прогресса у карточки сектора считается по настоящим `SaveService.getCompletedLevels()`. Плашка «ВЫГОДНО» ставится не на выбранный вручную набор, а на пакет с максимумом кредитов за единицу валюты по числовому `priceValue` из каталога — и не ставится вовсе, пока каталог не полон. **«Как играть» (4f)** — пять карточек-картинок вместо пяти строк текста; клавиши рисуются существующим `buildKeyRow`/`TutorialHints`, то есть на телефоне показываются экранные кнопки, а на клавиатуре — все три работающих биндинга, ровно как в подсказках первого уровня. Карточка ловушки — два кадра одного момента (телеграф ≥250мс, затем удар), то есть сама формулировка CLAUDE.md #4.2. **Настройки (4g)** — тумблеры и разделы вместо строк «ЗВУК: ВКЛ». Осознанные отличия: раздельных ползунков музыки и эффектов нет (звук — одна синтезируемая шина с одним флагом `AudioSettings`), тумблера «голос SYSTEM» нет (у комментатора нет выключенного состояния — это часть игры по §6), «СБРОСИТЬ ПРОГРЕСС» не перенесён: разрушающее действие без экрана подтверждения хуже, чем его отсутствие. **ESC закрывает любой оверлей** — новый `ui/ScreenChrome.ts` (общий топ-бар, полосы разделов, `attachEscape`) подключён к настройкам, «Как играть», выбору уровня и магазину; в магазине ESC из окна кредитов возвращает в магазин, а не наружу. Подтверждено вживую Playwright'ом на 480 и 620px: ноль ошибок консоли, ESC закрывает все четыре экрана, DOM-слоёв после закрытия не остаётся. Своя часть бандла — ~251 КБ (было ~236 КБ). |

| Третий разбор макета: пиксельный шрифт во всём UI, карта уровней (4e), SYSTEM на любой ширине | 2026-09-10 | 1.67 МБ | `phaser-*.js` — 1.41 МБ | **Шрифт.** Макет набирает всю техническую типографику (заголовки, счётчики, цены, голос SYSTEM) пиксельной гарнитурой и только прозу — пропорциональной; первый заход рисовал системным сансом всё. Теперь техническая половина идёт собственным bitmap-шрифтом проекта (`art/font`), который остаётся резким после nearest-neighbour-апскейла канваса, а проза — системным стеком (CLAUDE.md #3 прямо разрешает его для длинных блоков и запрещает веб-шрифты, поэтому Pixelify Sans/Rubik из макета не тянутся, а заменяются этой парой ролей). Битмап-шрифт масштабируется только целыми шагами, поэтому кегли макета отображены как 9-12px -> ×1, 15-19px -> ×2, 22-26px -> ×3. В `glyphs.ts` добавлены «+» и «·» — раньше `drawLines` молча пропускал их, и «+20» читалось как «20». Строка цены из каталога Яндекса осталась DOM-текстом: она приходит в рантайме и может нести любой символ валюты, которого в битмап-шрифте нет. **Раскладка магазина стала адаптивной**: колонка SYSTEM больше не исчезает — `ShopScene.layout()` держит её на любой поддерживаемой ширине, ужимая панель примерки и сетку карточек, а не выбрасывая колонку (на 480px её не было вообще). Сетка сама считает число колонок от оставшегося места, спрайт карточки ужимается под свою ячейку, имя предмета — под свободное место рядом с бейджем редкости. **Реплика SYSTEM теперь про конкретный предмет**, а не про категорию: `commentOnShopItem` в `data/dialogues/shop.ts` — по одной авторской строке на каждый id из `SHOP_ITEMS`, с откатом на категорию для будущих предметов. **Галочка** перерисована как пиксель-арт «лесенкой» по целым пикселям: повёрнутый `fillRect` (буквальный перенос CSS-трансформа из макета) давал сглаженные края, которые апскейл превращал в грязь. **Кнопка КУПИТЬ** потеряла обводку и подбирает кегль под свою ширину. **Скорость превью трейла** уполовинена: на 130px сцены реальная `PHYSICS.moveSpeed` читается дёрганьем, а не бегом. **Окно кредитов** перестало налезать на колонку SYSTEM, подпись «КРЕДИТОВ» — на кнопку цены. **Экран уровней (макет 4e)** переписан с шести одинаковых кнопок на карту сектора: пять маленьких карточек, одна крупная «ИГРАТЬ» на том уровне, где игрок реально стоит, витрина юнита, испытание дня с настоящим обратным отсчётом до UTC-полуночи и блок лучшего времени по сектору. Два осознанных отличия от макета: чипов в игре нет — вместо ряда чипов карточка показывает отметку прохождения и личное лучшее время из ghost-записи; замков нет — уровни не заперты, поэтому непройденный уровень получает приглушённый стиль макета, но не рисует замок, которого не существует. Экран стал полноэкранным, чем попутно чинится найденный владельцем баг: `MainMenuScene.openOverlay` прячет свой DOM-слой, и поверх старой полупрозрачной панели уровней пропадала подпись «СМЕНИТЬ ОБЛИК». Плитка «СМЕНИТЬ ОБЛИК» теперь открывает магазин сразу на вкладке ОБЛИК, а не на той, где его оставили в прошлый раз. Проверено вживую на 480 и 620px: ноль ошибок консоли, реплика SYSTEM меняется на каждый выбранный облик, покупка и навигация реальными кликами проходят, ESC закрывает все четыре экрана. Своя часть бандла — ~259 КБ (было ~251 КБ). |

| Четвёртый разбор макета: настоящие веб-шрифты вместо битмап-шрифта, найденный дефект Pixelify Sans | 2026-09-11 | 1.88 МБ | `phaser-*.js` — 1.41 МБ | Прямое требование владельца: «догружаем оригинальные шрифты, хоть и правило проекта запрещает, делаем, чтобы было красиво» — осознанное, явно санкционированное исключение из CLAUDE.md #3/#12 (задокументировано в `ui/fonts.ts`). Сначала подключили ровно то, что называет макет — `@fontsource/pixelify-sans` для всей технической типографики (заголовки/счётчики/цены/голос SYSTEM) и `@fontsource/rubik` для прозы. Результат оказался хуже собственного битмап-шрифта: буква «О» наезжала на следующий символ почти в каждом русском слове. **Найдена и подтверждена первопричина бинарным анализом** (`opentype.js`, вне игры, без участия кода проекта): `charToGlyphIndex('О')` и `charToGlyphIndex('П')` возвращают 0 (`.notdef`) в каждом весе обоих файлов пакета `@fontsource/pixelify-sans` — это не CSS-баг, не кернинг и не наша ошибка, а реальный дефект сборки шрифта у Fontsource: два самых частых письма русского алфавита физически отсутствуют. Проверка через CSS (`letter-spacing`, `font-kerning`, `font-feature-settings`) ожидаемо ничего не изменила — дело было в отсутствующем контуре, а не в шейпинге. Заменили на **Rubik Mono One** — однонасыщенная геометрическая гарнитура из того же шрифтового рода, что и прозаический Rubik; та же бинарная проверка подтвердила полное и корректное покрытие кириллицы. Побочный эффект: Rubik Mono One заметно шире Pixelify Sans на тех же кеглях, из-за чего несколько мест макета, тесно подогнанных под узкий пиксельный шрифт, перестали помещаться — каждое исправлено по факту живой проверки, а не заранее угадано: (1) подзаголовок топ-бара («СИСТЕМНЫЙ АРХИВ»/«ВАЛЮТА ДЛЯ ОБЛИКА И ТРЕЙЛОВ») наезжал на заголовок — оказалось, гонка загрузки шрифта: ширина заголовка измерялась ДО того, как реальный шрифт подменил `font-display:swap`-заглушку, и положение больше не пересчитывалось; `BootScene` теперь ждёт `document.fonts.ready` (через новый `fontsReady()`) перед стартом меню, а `ShopScene.retitle()` пересчитывает позицию подзаголовка при каждой смене заголовка («Магазин» ↔ «Кредиты»). (2) Бейдж редкости и имя предмета в панели примерки считали свою ширину по формуле «6px на символ», подобранной под старый шрифт — теперь оба измеряются напрямую через реальный `DomTextHandle.width`, а имя предмета дополнительно ужимается по кеглю, если не помещается («СЛЕД ДАННЫХ» рядом с бейджем «БАЗА»). (3) Имя предмета на самой карточке в сетке (не в панели примерки) не имело вообще никакого ограничения ширины — на двухколоночной сетке узкого канваса «СЛЕД ДАННЫХ» наезжало на соседнюю карточку «ЗАПУСК»; добавлено то же измерение+ужатие. (4) Строка «ОСТАНЕТСЯ N»/«НЕ ХВАТАЕТ N» рядом с трёхзначной ценой физически не помещается в панель ни при каком кегле вплоть до нечитаемого — добавлен короткий вариант по той же модели сокращений, что уже есть в игре («БЕЗ РЕК.»): «ОСТ. N»/«НУЖНО N», используется только когда полная фраза не влезает даже на минимальном кегле. (5) Заголовок панели примерки для SYSTEM-паков «ТРИ РЕПЛИКИ ПОДРЯД» переписан в «3 РЕПЛИКИ» — короче и без потери смысла. (6) Вкладки-переключатели «БЕЗ РЕК.»/«SYSTEM ACCESS» получили ту же пару «измерить-ужать» плюс жёсткий `wordWrapWidth` снизу — на самом узком поддерживаемом канвасе слово ужимается до «SYSTEM…», но никогда не залезает в соседнюю панель. (7) Реплика SYSTEM в правой колонке нигде не имела `clampLines` — длинная реплика на узком канвасе прежде переливалась из своего блока прямо в «СОБРАНО ОБЛИКОВ» под ним; теперь число строк считается от реально доступной высоты блока для обеих раскладок (персонажи/остальные категории, вкладка «Без рекламы»). Круглая монета магазина (кнопка КУПИТЬ, кошелёк, цены, пачки монет) перерисована как настоящий DOM-круг (`DomTextOverlay.addShape`) вместо `Graphics.fillCircle` — окружность на 270px-канвасе после nearest-neighbour-апскейла читалась гранёной, а не круглой (замечание владельца). Галочка «надето» и кнопка КУПИТЬ остались как в третьем раунде (пиксель-арт лесенкой, чёрный текст на золотой заливке — именно чёрный, белый оказался нечитаем на жёлтом фоне, замечание владельца). Проверено вживую полным перебором каждого предмета в каждой из пяти категорий на 480 и 620px (`shot-full-sweep.mjs`) — ноль ошибок консоли, ноль визуальных наложений; регресс по кликам и ESC зелёный; 631/631 тестов. Своя часть бандла — ~262 КБ (было ~259 КБ), плюс ~211 КБ шрифтов (`.woff`/`.woff2`, Cyrillic+Latin, только используемые начертания) — итог 1.88 МБ из 3 МБ. |

| Пятый разбор макета: пиксельный шрифт с полной кириллицей, разрывы слов, столкновения в шапке | 2026-09-11 | 1.90 МБ | `phaser-*.js` — 1.41 МБ | **Разбор начат заново, с нуля, по прямому требованию владельца** («правок 5-6, ничего не меняется, сверься с макетом полностью») — и первым делом опровергнут собственный вывод прошлого раунда. Проверка Pixelify Sans в прошлый раз шла через `opentype.js` с двумя ошибками сразу: устаревший `loadSync` возвращал `undefined`, а `Buffer.buffer` отдавал весь пул Node, а не файл, — то есть разбирался мусор. При корректном чтении (плюс контроль: латинский файл честно показывает 66/66 отсутствующих кириллических букв) картина такая: в кириллическом подмножестве Pixelify Sans нет **ровно двух** заглавных букв — «О» (U+041E) и «П» (U+041F), из всех 33. Строчные «о»/«п» на месте. Браузер молча подставляет их из системного шрифта, и посреди плотного пиксельного слова оказываются две тонкие полые буквы — это и есть «буквы побитые, внутри них дырка». Подтверждено тремя способами: разбором бинарников, рендером всего алфавита в Chrome при 44px и — решающее — загрузкой шрифта напрямую с `fonts.googleapis.com` ровно так, как это делает сам макет: **дефект присутствует и в макете**, поэтому воспроизводить его нельзя. Вывод прошлого раунда («заменить на Rubik Mono One») был верным по факту дефекта, но неверным по лекарству: Rubik Mono One вообще не пиксельная гарнитура и шире Pixelify Sans на ~35% (0.85em против 0.632em на букву), из-за чего переполнилась половина экранов. Теперь пиксельная роль отдана **Handjet** — настоящая пиксельная гарнитура, кириллица полная и проверенная (все буквы, цифры и «·» в обоих весах), при этом **уже** Pixelify Sans (0.432em), то есть она может только разгрузить вёрстку, но не переполнить её. Веса 500/700 (не 400/700): Fontsource'овские subset-файлы не несут ни `unicode-range`, ни запасных весов, поэтому незагруженный вес не откатывается, а **синтезируется** — глиф размазывается по горизонтали без увеличения ширины, и буквы налезают друг на друга; именно это, а не дефект шрифта, давало «наложение О» в самом первом заходе. Список весов вынесен в `PIXEL_WEIGHT`/`PROSE_WEIGHT`, чтобы вес нельзя было назвать в CSS, не загрузив его. Починены три системных бага, найденных по живым скриншотам: (1) `DomTextOverlay` ставил `word-break: break-word` — legacy-алиас, который рвёт **внутри** слова по первому непомещающемуся символу; отсюда «ИНТЕРЕСН/О,», «ПРИДУМ/АЛИ», «РЕКЛАМ/У» в колонках SYSTEM. Заменено на `word-break: normal` + `overflow-wrap: break-word` — перенос по словам, разрыв слова только если оно не помещается в строку целиком. (2) Подзаголовок шапки ставился в `40 + title.width` и ничем не ограничивался справа, поэтому «СЕКТОР 03 · INDUSTRIAL CORE» въезжал в счётчик «0 / 6». Правая метка теперь строится первой, её реальная ширина измеряется, и подзаголовок получает ровно оставшееся место (или не рисуется вовсе — он поясняет, а не инструктирует, в отличие от счётчика прогресса). (3) Прозе SYSTEM навешивался трекинг `letterSpacing: 1`, хотя макет даёт трекинг только **меткам**, а бегущим строкам — `line-height: 1.5` без трекинга. Отступы по замечанию владельца («слишком вытянутый, текст вылазит за границы»): `LevelSelectScene.fit()` **угадывал** ширину как «0.62em на символ» — заниженная оценка для заглавной кириллицы в Rubik 800, из-за чего «ИСПЫТАНИЕ ДНЯ» вылезало за рамку своей карточки; заменено на реальное измерение живого лейбла (ширина линейна по кеглю, поэтому хватает одного замера, без цикла подбора). Плитки уровней держали номер вверху, а подпись — прибитой к низу, оставляя 40px пустоты посередине каждой из шести; номер и статус собраны в одну центрированную группу. Блок «ЛУЧШЕЕ ПО СЕКТОРУ» начинал значение на 20px ниже метки — подтянут вплотную, как в макете. Чипы «СОБРАНО ОБЛИКОВ» при фиксированных 9px оставляли шестой чип одиноко на второй строке — размер чипа теперь считается так, чтобы весь набор лёг в одну строку (с нижней границей, чтобы будущий длинный каталог переносился аккуратными полными рядами), а счётчик «1 / 6» встал прямо под ними. Проверено вживую полным перебором пяти категорий магазина на 480 и 598px и экрана уровней на 480/598/598-при-860px (разрешение владельца) — ноль ошибок консоли, ноль наложений и ни одного разрыва слова; 631/631 тестов, typecheck и lint зелёные. Своя часть бандла — ~263 КБ, шрифты ~197 КБ (Handjet 500/700 + Rubik 400/700/800, Cyrillic+Latin, `.woff`/`.woff2`) — итог 1.90 МБ из 3 МБ. |

| Новый дизайн (ход 5): JetBrains Mono, экран паузы в трёх ширинах | 2026-09-11 | 1.97 МБ | `phaser-*.js` — 1.41 МБ | Владелец загрузил обновлённый макет по отправленному запросу-правке. Два изменения. **Первое: шрифт.** Pixelify Sans убран из макета полностью (0 вхождений), вся техническая типографика переведена на **JetBrains Mono** — это прямое следствие найденного дефекта: в кириллице Pixelify Sans отсутствуют ровно две заглавные буквы из 33, «О» (U+041E) и «П» (U+041F), и браузер подставляет их из системного шрифта. JetBrains Mono проверен тем же методом (разбор бинарников через opentype.js): кириллица полная во всех четырёх весах, латиница, цифры и «·» тоже; ширина 0.600em против 0.632em у Pixelify Sans, то есть измерения макета переносятся без подгонки. Handjet (временная замена прошлого раунда) удалён. Веса взяты те же, что в макете: JetBrains Mono 500/700, Rubik 400/800. **Второе: экран паузы** (разделы 5a/5b) — первый экран, нарисованный сразу в трёх ширинах (620/550/480) и снабжённый таблицей координат и правил, как и просил запрос. `PauseScene` переписан с трёх одинаковых кнопок на лестницу из трёх весов (236×56 светлая «ПРОДОЛЖИТЬ» → 236×44 «ЗАНОВО» → 236×32 «ГЛАВНОЕ МЕНЮ», заливка от светлой к почти фоновой), плюс блок цифр, по которым принимается решение о рестарте: время попытки, число попыток и личный рекорд сектора. Иконки — примитивы (треугольник, замкнутая стрелка из четырёх прямоугольников, П-образная рамка), не спрайты. **Два расхождения со спецификацией разрешены в пользу фактов.** (1) Формула карточки в тексте спецификации — `cardW = min(540, W − 24)` — противоречит её же таблице и рисунку при 550 (526@12 против нарисованных 470@40). Реализована форма `cardW = max(456, W − 80)`, которая воспроизводит все три нарисованные ширины точно: 620→540@40, 550→470@40, 480→456@12. (2) Спецификация задаёт выбор «длинная или короткая фраза» порогами по ширине холста, потому что описывала три ширины; применённые буквально, они ошибаются на всех промежуточных — при 598px в блоке 256px реплика, которой нужно 172px, всё равно получала бы короткий вариант. Все три таких решения (одно- или двухстрочные часы, полная или короткая метка рекорда, длинная или короткая реплика SYSTEM) принимаются **измерением живого лейбла**, а не порогом. **Одно осознанное отступление от макета по содержанию**: макет ставит пару тумблеров «МУЗЫКА/ЭФФЕКТЫ», но в игре один аудиопереключатель и нет музыкального слоя (о чём прямо сказано в комментарии `AudioSettings`); рисовать второй тумблер, который ничего не переключает, — это placeholder-контент, запрещённый CLAUDE.md #12. Второй слот занял тумблер тряски экрана — тот самый, который сам макет переносит в эту карточку на узких ширинах. Ярлык сокращён до «Тряска»: «Тряска экрана» не помещается в 93px-бокс при 480. **Две регрессии от более широкого шрифта найдены и починены по живым скриншотам**, обе одного рода — координата следующего элемента считалась от предположения, что метка займёт одну строку: «ЛУЧШЕЕ ПО СЕКТОРУ» на экране уровней (текст ниже печатался прямо через него) и «СОБРАНО ОБЛИКОВ» в магазине (чипы вплотную к метке). Оба теперь ставятся под **измеренную высоту** метки. Там же заголовок «БЕЗ РЕКЛАМЫ НАВСЕГДА» упирался в границу панели при 480 — кегль подбирается измерением вместо порога `offerW >= 240`. Проверено вживую: пауза на 598 и 480 (переключение тумблера, Esc-возврат в геймплей, отсутствие следов DOM-слоя после закрытия), пять категорий магазина на 480 и 598, экран уровней на 598 при 860px — ноль ошибок консоли, ноль наложений. 631/631 тестов. Своя часть бандла — ~266 КБ, шрифты ~265 КБ (JetBrains Mono 500/700 + Rubik 400/700/800, Cyrillic+Latin) — итог 1.97 МБ из 3 МБ. |

| Ресайз окна, ровная галочка, обводка мелкого текста | 2026-09-11 | 1.97 МБ | `phaser-*.js` — 1.41 МБ | Три замечания владельца, из которых второе оказалось самым тяжёлым багом за всё время работы над UI. **(1) Раскладка рассыпалась при изменении размера окна.** `ScaleController` меняет виртуальную ширину на каждый `resize`/`orientationchange` (высота 270 фиксирована, ширина плавает 480–620, CLAUDE.md #2), но **ни одна сцена не перестраивала раскладку** — каждая измеряла ширину один раз в `create()` и раскладывала панели от неё. После ресайза экран оставался свёрстан под ту ширину, при которой его открыли: панели уезжали за край, а DOM-слой, который как раз следит за прямоугольником канваса, продолжал масштабироваться правильно — отсюда впечатление «шрифт отличается» и «дизайн полностью поехал». Воспроизведено вживую ресайзом 1240×560 → 1920×965. Добавлен `ui/relayout.ts`: перестроение через `scene.restart()` при реальной смене виртуальной ширины, подключено ко всем шести меню-сценам и паузе. Геймплей намеренно не подключён — уровень не должен перезапускаться под игроком, и ему это не нужно: камера просто показывает больше мира. По ходу вскрылись два связанных бага: (а) перезапуск оверлея шлёт `SHUTDOWN`, и `MainMenuScene` принимал это за закрытие магазина — показывал свой DOM-текст поверх него; теперь перезапуск отличается от закрытия проверкой `scene.isActive` тиком позже, а меню под открытым оверлеем откладывает собственное перестроение до момента, когда его раскроют (`RelayoutHandle.flush`); (б) `buildSystemLine` выходит досрочно при узком слоте и не переприсваивает `systemLineLabel`, так что после перестроения обработчик реплик SYSTEM писал в уничтоженную метку и ронял страницу — поле сбрасывается в `create()`. **(2) Галочка.** Две разные реализации (магазин и уровни) рисовали её колонками разной высоты (2,2,1,1,2,2,2) — контур выходил щербатым. Сведены в одну `ui/glyphs.ts` с равной толщиной штриха `2u` во всех семи колонках: две диагонали одного веса, как в макете (там это CSS-бокс с левой и нижней границей, повёрнутый на −45°; буквально повторить нельзя — повёрнутый прямоугольник на 270px-канвасе после nearest-neighbour-апскейла превращается в грязь, поэтому рисуется ступенькой по целым пикселям). **(3) «Побитый текст с дырками внутри букв».** `DomTextOverlay` задавал обводку одновременно `-webkit-text-stroke` и `text-shadow`; первое рисуется **по центру** контура, то есть половина толщины уходит внутрь буквы, а в жирном начертании на UI-кеглях просветы «а», «о», «е», «в», «я» шириной всего пара пикселей — обводка их закрывала. Вдобавок комментарий считал `text-shadow` фолбэком для Firefox, но Blink рисует оба, удваивая порчу. Центрированная обводка убрана совсем, остался один жёсткий теневой сдвиг и только начиная с 20 CSS-px, где пиксель уже не составляет трети просвета. Это соответствует макету: он не обводит текст нигде, `text-shadow` там только свечение и один крупный декоративный сдвиг, а каждая подпись лежит на сплошной панели. Проверено вживую на 480 и 598px: пять категорий магазина, уровни, настройки, пауза, ресайз окна на каждом экране — ноль ошибок консоли, ноль наложений. 631/631 тестов. Размер бандла не изменился — 1.97 МБ из 3 МБ. |

| Плавные глифы на DOM-слое, настоящие замки уровней | 2026-09-12 | 1.97 МБ | `phaser-*.js` — 1.41 МБ | **Глифы.** Галочка, треугольник «играть» и ромб рисовались в канвас, который апскейлится nearest-neighbour'ом, поэтому любая диагональ приезжала лесенкой; галочку до этого пытались чинить ручной пиксель-лесенкой, и она всё равно читалась щербатой. Теперь все три строятся ровно так, как их строит макет — обычными CSS-боксами (галочка: коробка с левой и нижней границей, повёрнутая на -45°; треугольник: нулевой бокс с прозрачными верхней и нижней границами; ромб: квадрат, повёрнутый на 45°) — и живут на DOM-слое, который браузер рисует в нативном разрешении экрана, уже после апскейла канваса. `DomTextOverlay.addShape` получил `rotate` и границы по сторонам; помощники — в `ui/glyphs.ts`. Переведены: все галочки (карточка магазина, кнопка «надет», строки премиума, пачки кредитов, плитка пройденного уровня), треугольники (меню, карта уровней, пауза, кредиты) и ромбы (рейл магазина, заголовок премиума, испытание дня, «сменить облик»). Иконки меню из прямоугольников (уровни, магазин, справка, настройки) остались на канвасе — прямые штрихи апскейл переживают как раз хорошо, им нужна пиксельная резкость. Диагональные иконки меню отмечены в `DIAGONAL_ICONS`, `MenuIcon` их не рисует, а `MenuTile` теперь сообщает наружу координаты пиктограммы и её цвет — тем же механизмом, которым уже сообщал состояние подписи, так что DOM-глиф проседает и перекрашивается вместе с кнопкой при наведении и нажатии. Мёртвые `drawPlay`/`drawSkin` удалены. **Замки уровней — настоящие.** Экран рисовал карту сектора, но уровни ничем не запирались: шестой открывался сразу с чистого сейва, и замок из макета был бы нарисованной ложью (CLAUDE.md #12). По решению владельца введена последовательная прогрессия: `isLevelUnlocked` в `gameplay/sectors.ts` открывает уровень, когда пройден предыдущий, через границу сектора включительно (перед `sector-02-level-01` стоит `sector-01-level-06`). Функция чистая — берёт `isCompleted` параметром, а не импортом, поэтому покрыта тестами без обращения к хранилищу (`tests/level-unlock.test.ts`, 8 проверок). Запертая плитка рисует замок макета (корпус плюс дужка из трёх прямоугольников) над приглушённым номером и **не получает hit-зоны вообще** — она не отвечает на тап, а не просто выглядит запертой. **«ИГРАТЬ» вылезало за край карточки**: `fit()` мерил пробником без `bold`, а надпись жирная, и Rubik в тяжёлом начертании шире — оценка занижалась. Пробник теперь в том же начертании, что и сама надпись. Проверено вживую на 480 и 598px: меню, пять категорий магазина, карта уровней, пауза — ноль ошибок консоли, ноль наложений. 639/639 тестов, 1.97 МБ из 3 МБ. |

| Десять замечаний по живой игре: часы паузы, экран результата, плавные стрелки | 2026-09-12 | 1.98 МБ | `phaser-*.js` — 1.41 МБ | Разбор десяти замечаний владельца, два из которых оказались настоящими багами логики, а не оформления. **(1) Треугольник «играть» превращался в квадрат при наведении.** `DomTextHandle.setColor` для фигур всегда красил `background`, а треугольник — это нулевой бокс, у которого верхняя и нижняя границы прозрачные: фон рисуется по границе (`background-clip: border-box`) и проступает сквозь них, заливая весь бокс. Теперь `setColor` красит то, чем фигура на самом деле нарисована: стороны (кроме намеренно прозрачных), рамку или фон. **(2) Часы попытки шли во время паузы.** `GameState.elapsedMs()` — настенные часы (`performance.now()`), они обязаны переживать `scene.start` между уровнями, поэтому пауза сцены их не останавливала. Добавлены `pauseClock`/`resumeClock` с накоплением `pausedMs`; `GameplayScene` останавливает часы при паузе, `PauseScene` перезапускает их из своего `shutdown` — один раз на все три выхода (продолжить/заново/меню). Порядок при «Заново» проверен: `scene.stop()` ставит shutdown в очередь, так что новый `startRun()` проходит раньше, и свежая попытка начинает счёт ровно с момента закрытия карточки. Заодно вскрылся более старый баг: `currentLevelId` инициализировался значением `sector-01-level-01`, то есть именно тем уровнем, на который открывается чистый сейв, — проверка «уровень сменился?» при первом запуске сессии говорила «нет», `startRun()` не вызывался вовсе, и карточка паузы показывала время с момента загрузки страницы (минута против пяти секунд на HUD). Поле теперь пустое до первого входа в уровень. Покрыто четырьмя тестами. **(3) Экран «сектор пройден» переведён на DOM-слой** — последний экран, чей текст оставался в канвасном битмап-шрифте, и это было видно: «МЕНЮ» и «СЕКТОР 2» под «ДАЛЬШЕ» превращались в кашу после апскейла. Заодно там же не хватало треугольника на главной кнопке (регрессия прошлого раунда: `play` попал в `DIAGONAL_ICONS`, а DOM-глиф этой сцене не добавили) и правая колонка висела на `width - 128`, что расходится с содержимым экрана при любой ширине меньше 608 — при 598 «ПРОГРЕСС» печатался прямо через лидерборд и кнопку. Колонка закреплена за контентом и берёт остаток ширины, как колонка SYSTEM в магазине. Лидерборд теперь чистится перед перерисовкой — повторная загрузка после авторизации печатала второй набор строк поверх первого. **(4) Кнопки паузы не отвечали на наведение.** Карточка рисовала три действия один раз и больше не перерисовывала; теперь у каждой кнопки своя `paint(hover, press)` — подсветка, обводка и проседание при нажатии, как у плиток главного меню, плюс обводка у тумблеров. **(5) Плавные стрелки.** Шевроны «назад» (магазин, уровни, настройки, справка) и стрелки перелистывания секторов рисовались `lineStyle` в канвас и приезжали лесенкой; переведены на DOM-глиф `addChevronGlyph` — тот же CSS-бокс с двумя границами, что и галочка, повёрнутый на 45°. Коробка вокруг стрелки осталась на канвасе: она прямоугольная, ей апскейл к лицу. **(6) SYSTEM рвал слова** («ПОНРАВИТЬС / Я.», «НЕЗАВЕРШЕ / НО.»). `overflow-wrap: break-word` — последнее средство браузера: слово, которое не помещается в строку целиком, разрезается посередине. Причина не в переносе, а в кегле: слово шире своего бокса. Теперь **любая** метка с `wordWrapWidth` измеряет своё самое длинное слово и уменьшает кегль (не более чем на 40%), пока слово не поместится целиком; браузерный разрыв остался внизу как аварийный. **(7) Убран остаток в магазине** — строка «ОСТАНЕТСЯ 680» рядом с ценой считала за игрока то, о чём он не просил, и дублировала кошелёк в шапке. Осталось только сообщение о нехватке. **(8) Превью «ФИНАЛ» было сломано на узкой панели**: две сцены по 62px не помещаются в 110px между границами, они перекрывались на 14px и читались как один бокс с полосой посередине, а стрелка «до → после» начиналась внутри правой сцены и печаталась поперёк андроида. Ширина сцен считается от панели, стрелка центрируется в реальном зазоре и не рисуется, если зазора нет. **(9) Свет вылезал за блок предпросмотра**: конус (48px) и световая лужа (44px) были нарисованы под широкую панель с 56px-сценой, а на узкой сцена 40px. Обе величины считаются от бокса с отступом 3px. **(10) Эффекты смерти не отличались друг от друга** — три варианта различались только числом полос и частиц. У каждого теперь свой почерк: `static` — красные осколки и красная вспышка; `glitch` — цветоделение, голубая и фиолетовая полосы, растащенные в разные стороны, холодная вспышка и более плотный короткий выброс; `data_wipe` — белая полоса-стиратель, проходящая сквозь андроида под удержанной белой вспышкой. Цвет осколков тоже идёт от варианта. Попутно: заголовок «ИСПЫТАНИЕ ДНЯ» упирался в рамку — карточка расширена до ширины кнопки «ИГРАТЬ» над ней (и там же выбор полной/короткой формы по измерению, с порогом читаемости 9px), обратный отсчёт вылезал за рамку при 480 — подгоняется измерением; галочка на кнопке магазина создавалась заново внутри `redraw`, то есть на каждое наведение добавлялся ещё один DOM-элемент — создаётся один раз и переставляется. Проверено вживую на 480 и 598px: меню, пять категорий магазина, карта уровней, настройки, справка, пауза (с замером, что время паузы не попадает в счёт), экран результата — ноль ошибок консоли, ноль наложений, ни одного разрыва слова. 643/643 теста, 1.98 МБ из 3 МБ. |
| Пересмотр формата уровня: один экран, вся кампания переписана | 2026-09-12 | 1.96 МБ | `phaser-*.js` — 1.41 МБ | Уровень стал ровно `LEVEL_WIDTH_TILES` (48 тайлов = 480 px, то есть самый узкий возможный экран) на 27 тайлов, камера больше не прокручивается. Все 30 уровней переписаны с нуля: сложность теперь от расстановки и точности, а не от длины, уровни строятся вверх (примерно семь ярусов на высоту экрана, шаг 3 ряда — окно по времени отталкивания ≈ 100 мс, проверено вживую). Новый `exitRow` ставит выход на ярус, превращая уровень из коридора в подъём. Размер стал меньше прежних 1.98 МБ, хотя контента столько же: 250-тайловые ручные раскладки секторов 01–02 были заметно объёмнее одноэкранных, а вместе с чекпоинтами, `LevelSections` и полосой прогресса в HUD ушёл и их код. **Три настоящих бага, найденных по дороге, а не на глаз.** (1) Первая версия масштабировала мир под ширину экрана — неверно: зум одинаков по обеим осям, поэтому горизонтальная подгонка покупается вертикальной обрезкой, и на самом широком экране было видно 209 рядов из 270 с землёй за нижним краем. Теперь зум всегда 1, уровень по центру, лишнюю ширину закрывают боковые стены с бирюзовой кромкой — широкий телефон показывает больше декорации, но никогда больше уровня. (2) Солвер достижимости был ненаправленным и считал любые две площадки с пересекающимися колонками взаимно достижимыми независимо от высоты: безобидно для плоского коридора, для которого он писался, и полностью неверно для подъёма — уровень с недосягаемым выходом прошёл бы проверку. Рёбра стали направленными, а подвижная платформа — поездкой между двумя узлами вместо одной точки, иначе честный мост через широкую пропасть признавался непроходимым. (3) Полоса прогресса и `progressFraction` для реплик SYSTEM считали `x / ширина` и на вертикальном уровне объявляли бы «почти дошёл» игроку, стоящему внизу лестницы; полоса удалена, комментатор теперь меряет расстояние до двери. Добавлен `pnpm levels` — ASCII-раскладка всех 30 уровней с вердиктом солвера; именно она поймала рассыпающуюся платформу, вплотную приставленную к обычной в финале. Проверено вживую на 480 и 620 px: меню, магазин, карта уровней, десять уровней из пяти секторов — ноль ошибок консоли. 609 тестов. |
| Порядок обучения и починка портала | 2026-09-12 | 1.96 МБ | `phaser-*.js` — 1.41 МБ | Кампания переставлена под нарастающую сложность по просьбе владельца: по одной идее на уровень, от телесного к абстрактному. Сектор 01 — пол проваливается, шип ходит поперёк, дыра в полу перемещается, подъём; сектор 02 — всё, что рассыпается и качается; 03 — часы (лучи, поршни, стены); 04 — обман (фальшивый уступ, электропол, петля, ambush-шипы); 05 — давление (преследователь, фальшивый выход, гонтлеты). «Движущаяся дыра» — существующий `moving-platform`, взятый наоборот: пропасть 20 колонок, плита 14, читается дыра, а не мост; та же ловушка в секторе 02 размерена наоборот и читается мостом. Рампа в числах: в `DROP` провалы 2 → 3 → 6 колонок, непрыгаем только последний. Варианты переписаны, `troll` теперь сужает тот самый непрыгаемый провал на колонку. **Портал перестал дёргаться**: он анимировался твином `scale: 1 → 1.04` с центральным origin — на 50-пиксельном спрайте под `roundPixels` это 2 px, прыгающие между целыми, и рост от центра топил нижнюю кромку двери в пол на пиксель каждый цикл. Теперь спрайт прибит низом к линии поверхности, а пульсирует свечение, а не геометрия. 611 тестов, проверено вживую на девяти уровнях — ноль ошибок консоли. |
| Ловушки, которые срабатывают при подходе | 2026-09-12 | 1.96 МБ | `phaser-*.js` — 1.41 МБ | Три замечания владельца по живой игре. (1) «Текст не влезает в поле» на экране результата сектора: плашка «НОВЫЙ РЕКОРД» была прямоугольником фиксированной ширины 52 px, а русская строка в неё не помещается — теперь плашка рисуется по измеренной ширине подписи, а сама подпись ужимается через `fit()`, если её не держит даже ширина панели. (2) «Первые 5 уровней беспонтовые, пол должен двигаться прямо передо мной, срабатывать как тригер шипа» и (3) «ловушка, где подо мной проваливается пол потихоньку, бесполезна абсолютно — нужны ловушки, которые срабатывают как тригер, когда я подхожу, и если я ничего не сделаю, то умру». Обе — про одно: `falling-platform` получил режим `armed`, в котором он не реагирует на наступание, а ждёт `trigger`, поставленный на четыре колонки раньше; пол мигает красным 350 мс и уходит вниз. Три захода до рабочей версии, каждый измерен живьём: пол, уходящий из-под ног, игрок на бегу просто перешагивает (свободное падение даёт 8 px за те 250 мс, что он пересекает три колонки), быстро уезжающая плита — то же самое. Работает третий: ловушка срабатывает так, что яма открывается за шаг ДО игрока, и он вбегает в неё. Сектор 01 переписан целиком вокруг этого, плюс триггерная банка шипов в PATROL и триггерный люк в GHOST FLOOR. Попутно из живой проверки: лестницы в ASCENT, BOOT COMPLETE и GHOST FLOOR сложены зигзагом (прямой подъём загонял портал в правый верхний угол под кнопку паузы), а финальный тир SYSTEM CORE опущен на ряд — дверь высотой 50 px на ряду 4 обрезалась верхним краем экрана. Размер не изменился: вся правка — данные уровней и один режим в существующей ловушке. |
| Девять замечаний по живой игре: ловушки прячутся, хитбоксы чинятся | 2026-09-13 | 1.96 МБ | `phaser-*.js` — 1.41 МБ | Девять пунктов от владельца после прохождения. **Видимость:** маркеры триггеров убраны совсем (`TriggerTrap` больше ничего не рисует), люк в полу получил тайл соседней земли и её же цианововую кромку, а `tile-ground-edge` перестал ставиться у ямы, которую он ещё накрывает — пол над ловушкой теперь неотличим от пола; `spike-bank` в покое невидим (alpha 0 вместо 0.35), появляется на фазе предупреждения. **Три настоящих бага, найденных его игрой, а не тестами:** (1) `moving-platform` центрировалась на одном тайле вместо того, чтобы начинаться с `fromCol`, из-за чего плита в 14 колонок заезжала на семь колонок поверх земли — это и было «блоки друг на друге» в SHIFT, и это же смещало каждую широкую плиту кампании относительно того, где её видит солвер; (2) её же тело задавалось через `setSize(widthPx)`, который Arcade умножает на масштаб спрайта — тело выходило в `widthPx/10` раз шире картинки, отсюда «отталкиваюсь от воздуха»; (3) у орбитальных, качающихся и петлевых шипов хитбокс стоял в основании тайла (для шипов, торчащих из пола), поэтому шип проходил сквозь игрока и не убивал. **Дизайн:** упавшая земля больше не возвращается (и солвер перестал считать опорой любую падающую платформу — уровни доказываются без них); FREEFALL перестроен в сплошную дорожку, где держится каждая вторая плита; ASCENT получил шип, падающий на следующий ярус в момент, когда игрок встал на предыдущий; преследователь ждёт 2 секунды и стартует с самого края; фальшивая платформа перерисована как полупрозрачный слэб с разорванной кромкой вместо тёмной коробки с оранжевыми чёрточками. Размер не изменился — правки в геометрии тел, отрисовке тайлов и данных уровней. |
| Шахта под люком и часы, которые считали паузу | 2026-09-13 | 1.96 МБ | `phaser-*.js` — 1.41 МБ | **Люк выдавал себя снизу.** Подземная порода рисуется одним `tileSprite` на каждый непрерывный участок пола, а колонки люка — это дыра в геометрии уровня, поэтому участки обрывались по обе стороны от люка и под совершенно обычным на вид полом зияла чёрная шахта во всю высоту экрана. Замер строки пикселей на 30 px ниже поверхности DROP: порода — rgb(11,11,20), провал — rgb(5,5,10) ровно на колонках 12–15, 22–25, 32–35 и 39–42, то есть на всех четырёх люках, до того как хоть один сработал. Теперь люк несёт собственную породу (`shaftDepthPx`) на глубине -1, выровненную по той же тайловой сетке, и прячет её ровно в тот кадр, когда плита начинает падать — шахта открывается по мере того, как пол в неё проваливается. Настоящие ямы по-прежнему чёрные: это их законный признак. **Часы в HUD не останавливались на паузе.** Они считались как `time - attemptStartMs`, где `time` — часы игрового цикла, а те тикают и при `scene.pause()`; минута, проведённая за карточкой паузы, возвращалась на экран сразу после продолжения. Теперь это сумма `delta` из `update()`: поставленная на паузу сцена не тикает вовсе, так что посчитать несыгранное время нечем. Проверено живьём — 0:04 до паузы, 0:04 через 5 секунд паузы, 0:06 через 2 секунды после продолжения. Размер не изменился — один `tileSprite` на люк и замена поля сцены. |
| Коробка поражения по видимому корпусу, осыпающийся пол, CRUMBLE | 2026-09-13 | 1.96 МБ | `phaser-*.js` — 1.41 МБ | **Шип проходил сквозь игрока.** Смертельность читалась с того же тела, что и коллизия, — а это 6×12 у самых ног при спрайте 24×36. Значит поранить андроида можно было только за ноги: голова и корпус, две трети того, что на экране, были призраком. Замер на PATROL: спрайт 24×36 (низ 220), тело 6×12 (208–220). Введена отдельная коробка поражения `hurtBounds()` — голова+корпус+ноги минус 2 px прощения с каждой стороны, руки снаружи; коллизионное тело не тронуто, потому что по нему выверены все ямы кампании. Смертельный контакт считается своей выборкой в `update()` по переиспользуемым прямоугольникам (ноль аллокаций), а не двумя `physics.add.overlap`. Попутно перенесён шип HUNTED: на ряду 19 он висел ровно на уровне груди — под ним не встать, через него неудобно прыгать, и работал он только потому, что не работал вовсе; теперь это наземный патруль, как в PATROL. Правило закреплено тестом (30 новых проверок, по одной на уровень). **Осыпающийся пол стал стоить решения:** падающая плита сначала мигает и трясётся `warnMs`, оставаясь опорой, и только потом отпускает — а отпустив, перестаёт быть опорой совсем (ни прыжка, ни coyote). Замерено: прыжок в фазе предупреждения даёт 31 px подъёма, после начала падения — 0. **CRUMBLE:** уступ теперь осыпается целиком, а не по плитке, по которой можно было просто идти; лестница стала четырьмя осыпающимися ступенями без единой твёрдой передышки, под верхней половиной — шипы. **ASCENT:** шип падает на колонку 23 (место приземления), а не на 21, куда игрок никогда не встаёт. **Экран завершения сектора** приходит одним движением: панели, кнопки и слой текста гаснут-проявляются за одни и те же 260 мс, без ступенчатой задержки, которую DOM-слой текста всё равно не мог отыграть. Размер не изменился. |
| Кампания перестаёт проходиться с первой попытки | 2026-09-13 | 1.96 МБ | `phaser-*.js` — 1.41 МБ | **Ловушки по триггеру были только в 8 уровнях из 30** — остальные 22 читались с экрана и проходились на терпении. Общий словарь засад вынесен в `data/levels/ambush.ts` (`dropSpike`, `floorSpikes`, `trapdoor`, `shiftingPit`, `approach`), где вся геометрия выводится из двух чисел — длительности предупреждения и выноса триггера вперёд на `moveSpeed × warningMs`, — а не ставится на глаз. Теперь засада есть в каждом уровне. Честность не нарушена: телеграфируется смертельное состояние, а не факт существования ловушки. **Новый механизм — уезжающая яма** (`moving-platform` с `armed`): плита стоит, пока её не запустит триггер, потом одним рывком уезжает и остаётся там. Видишь дыру, целишься перепрыгнуть, а дыра переезжает ровно туда, куда ты метил. Солвер считает такую плиту только в конечном положении — уровень доказывается в том состоянии, в котором ловушка его оставляет. Дебютирует в SHIFT. **Пол больше не едет лифтом:** вместо постоянных 100 px/с — короткий толчок и ускорение 1100 px/с²; замерено, 243→279→321→384 по 100 мс. **Шипы из земли бьют, а не выезжают:** `Quint.easeIn` тратит три четверти окна почти на месте и последнюю четверть на весь подъём; замерено, 235→234→230→218→215. **PENDULUM:** убраны наземные шипы из-под качелей — там было единственное место, где нужно стоять и ждать. **CRUMBLE:** обе стороны лестницы прикрыты засадами. Добавлено 60 тестов (проверка размещения каждой засады и уникальности id). Размер не изменился — данные уровней и тайминги. |
| Падающий пол без предупреждения | 2026-09-13 | 1.96 МБ | `phaser-*.js` — 1.41 МБ | Фаза предупреждения у `falling-platform` — тряска и красная вспышка перед тем, как пол отпустит — убрана по решению владельца («платформы не предупреждают о падении, а сразу падают»; на возражение о телеграфе — «делаем всё равно, всё равно на инвариант честности»). Исключение записано в CLAUDE.md #4.2 как его решение, а не растворено в коде. Состояния стали `armed → solid → doomed → falling → gone`, где `doomed` — это `holdMs` совершенно обычного, ничем не отличающегося пола (по умолчанию 0, то есть пола не станет в тот же кадр). `isSolid()` гаснет в момент отпускания, так что игрок просто падает сам, без поездки вниз. Из-за этого перестал срабатывать коллайдер, который отменял coyote-время, — и 100 мс прыжка из воздуха вернулись бы сами собой; отмена перенесена в покадровую выборку сцены по плитам в состоянии падения прямо под ногами. Замерено: люк уходит с кадра триггера (225→243→273→313→355→416), прыжок с исчезнувшего камня даёт 0 px подъёма. Варианты DROP переведены с `warnMs` на `holdMs` и только вверх: окно чтения теперь целиком геометрическое (вынос триггера на 4 колонки ≈360 мс с уже открытой ямой), а задержка его съедает, поэтому честного `bold` у этого уровня не осталось и он убран. Размер не изменился. |
| Два непроходимых уровня, триггеры на краю, читаемость | 2026-09-13 | 1.96 МБ | `phaser-*.js` — 1.41 МБ | **BRIDGE был непроходим по геометрии:** плита-паром ехала на три ряда выше земли, откуда садишься, а прыжок поднимает на 34.7 px — то есть верх плиты перекрывался на 4.5 px, и посадка требовала поймать два кадра на вершине слепого вертикального прыжка. Солвер этого не видел: он моделирует поездку как ребро между двумя концами плиты и ничего не знает о посадке. Плита переехала на ряд 21 и начинается у самого края ямы — шаг в 10 px. Проверено живьём: запрыгнул с края, проехал, сошёл на той стороне. **SYSTEM CORE:** маятник качался по колонкам 26.4-30.6 на высоте 53-68 — это весь обязательный ярус row 7 на уровне груди стоящего; стоять было негде ни в один момент качания. Перенесён, теперь ярус чист все 120 кадров замера, а безопасная площадка на row 10 — тоже. Ярус row 13 сдвинут вправо, чтобы подъём не висел на единственном осыпающемся уступе. **ORBIT:** радиус 1.75 укладывался целиком в промежуток между ярусами — шип мёл пустоту и не мог достать стоящего. При радиусе 3 достаёт до кромок обоих ярусов (замер: верх орбиты y 152 против коробки стоящего 128-160, попадание в 10 кадрах из 120). **Триггеры:** `TRAPDOOR_LEAD` 4→1 (люк открывается у самой кромки), `AMBUSH_TRIGGER_LEAD` 6→3 и `warningMs` 500→320 — ловушка становится смертельной в кадр прибытия, а не стоит наготове заранее. **Фиолетовый узел** на фоне переведён в фоновые значения: он был самым ярким насыщенным объектом на экране и читался как портал. **Фальшивые платформы** мерцают со сдвигом по плиткам — неподвижная полупрозрачная плита была загадкой про пиксели, мерцающая говорит «меня тут нет». Размер не изменился. |
| Триггер, который пропускал прыжок, и вариант, который глушил люк | 2026-09-14 | 1.96 МБ | `phaser-*.js` — 1.41 МБ | **Две независимые причины у одного симптома** («триггеры иногда не срабатывают сразу, и я могу пробежать пол»). Первая — варианты DROP: `gentle` держал последние два люка 900 мс, `troll` первый — 1300 мс. Замер до правки: игрок пересекает яму на колонках 12-14 по целому полу, добегает до 25-й, и только там пол отпускает. Это вариант, работающий ровно как написан, и всё равно баг — ловушка, которая на глазах не срабатывает, неотличима от сломанной. У DROP вариантов больше нет: это четыре одинаковых люка, и единственное, до чего вариант мог дотянуться, — `holdMs`. Правило закреплено тестом: вариант может менять скорость движущегося, но не может менять сам факт срабатывания (`holdMs`/`armed`/`loop` обязаны совпадать с базой). Вторая — высота полосы триггера: было 3 тайла, то есть 30 px над полом, а замер живьём показал, что на вершине прыжка тело игрока занимает **y 175-187** при поле на 220, то есть проходит выше полосы целиком. Высота теперь выводится из `MAX_JUMP_RISE_PX + PLAYER_BODY_HEIGHT` (5 тайлов). Побочно пришлось развести шесть засад, чью полосу подняло под платформу сверху — иначе прогулка по платформе тратила одноразовую ловушку впустую; тоже закреплено тестом. Размер не изменился. |
| Триггер как трамплин, устаревшая витрина, нечитаемая обманка | 2026-09-14 | 1.96 МБ | `phaser-*.js` — 1.41 МБ | **Главное: от триггера можно было оттолкнуться в воздухе.** Владелец: «если быстро быстро быстро кликать прыжок, то можно буквально летать и прыгать от воздуха. Обычно перед ямой так получается, будто от триггера отталкиваюсь» — и описание было буквально верным. Arcade выставляет игроку `touching.down` при любом разрешённом вертикальном пересечении, включая overlap-only, где флаг ставится, а тела не разводятся; `Player.preUpdate` читает этот флаг как «подо мной земля». Полоса триггера стала пятитайловой в прошлом раунде (`APPROACH_BAND_TILES`, чтобы ловить прыгающего) и стоит у самого края ямы — то есть превратилась в лестницу, по которой можно подниматься, пока тапаешь прыжок. Замер до фикса на DROP (`bd`=blocked.down, `td`=touching.down): `x108 y214 vy71 .T` → следующий кадр `x110 y210 vy-235` — старт прыжка в 6 px над полом. Ни типы, ни тесты об этом сказать не могли. Исправлено удалением физики у всех трёх детекторов игрока: `TriggerTrap`, `FakeExit` и выход уровня — теперь простые `Phaser.Geom.Rectangle`, которые сцена проверяет вручную в `sweepZoneContacts()` (тот же приём, что уже применён для смертельных контактов и падающего пола). В `GameplayScene` не осталось ни одного `physics.add.overlap`, и это закреплено новым тестом `tests/player-contact-invariants.test.ts`. После фикса на пяти уровнях с триггерами — ноль стартов прыжка в воздухе, все 30 уровней по-прежнему завершаются по выходу (живая проверка телепортом в дверь), фальшивый выход по-прежнему отказывает и не копит твины. **Витрина главного меню показывала устаревший инвентарь.** Магазин — оверлей, меню под ним не пересоздаётся, а пьедестал и баланс снимаются один раз в `create()`: сменил облик, вернулся — на пьедестале прежний андроид (и, по той же причине, прежний баланс после покупки). Добавлен `syncShowcase()` на закрытие оверлея; проверено живьём: `player-idle-0` / 0 кредитов → `player-void-idle-1` / 500. **Обманка-платформа не читалась как платформа.** Тело тайла рисовалось с alpha 0.4, кромка была разорвана на два штриха, а твин мерцания сверху умножал всё это на 0.25–0.85 — на экран попадала пунктирная линия на фоне, и владелец второй раунд подряд спрашивал, что это. Тайл перерисован силуэтом настоящей плиты (сплошное тело, целая кромка), единственный признак — фиолетовая кромка SYSTEM вместо конструкционной бирюзы плюс сканлайн; мерцание ослаблено до 1→0.55, чтобы силуэт читался всегда. |
| Наложенные ловушки, безвредный шип, неотличимая обманка | 2026-09-14 | 1.96 МБ | `phaser-*.js` — 1.41 МБ | **PISTON ROW: две машины на одних тайлах.** Триггерный банк был написан на колонках 31-33, третий пистон уже занимал 30-32 — «плохо срабатывают тригеры, шипы друг на друга налазят». Банк унесён на 36-38. Статический аудит нашёл ещё три таких пары (PATROL: падающий шип приземлялся в середину маршрута патруля; GAUNTLET: падающий шип на тайлах пистона; PRESSURE: пистон в колонке электропола) — все разведены, и теперь это тест: две опасности не могут делить и колонки, и ряды одновременно. **«Втупую пропрыгал весь уровень».** Пистон высотой в один ряд перепрыгивается прыжком на 34.7 px, то есть безопаснее всего было не касаться пола вовсе. Добавлены потолочные зубья над промежутками между пистонами на высоте, выбранной по замеру, а не на глаз: летальны на ряду 17 (y 170-180), голова стоящего андроида на y 188, прыгающего — на y 153. Живой замер: стоящий игрок 0 смертельных кадров из 480, прыгающий — 59. Плюс собственный цикл уровня (`PISTON_TIMING`): пистон наверху 700 мс из 1850 вместо 300 из 1950, телеграф не тронут (500 мс, вдвое больше `MIN_WARNING_MS`). Скриптовый прогон «зажать вправо, спамить прыжок» до правки выигрывал 4 попытки из 5, после — 0 из 5; при этом каждый пистон безопасен непрерывно 992-1280 мс против 318 мс, нужных на проход. **Падающий шип был безвреден в полёте** — это была прямая реализация телеграфа, и на экране она выглядела как шип, пролетающий сквозь игрока. Телеграф и полёт поменяны местами (`AmbushSpikeTrap.HANG_MS`): шип появляется, висит и дрожит `MIN_WARNING_MS`, затем падает смертельным. Замер: 13 кадров виден и безопасен, 7 кадров падения — все смертельны. Заодно летальный свип стал заметающим: хитбокс шипа 4 px, а падение покрывает 100 px за 310 мс, то есть за кадр шип перешагивал игрока целиком. **Красный шарик ждал по часам**, и это было неправильно в обе стороны: мгновенный старт убивал читающего экран, фиксированные 2000 мс дали «я буквально уже убежал с зоны его действия и он только заработал». Ждать надо было не время, а решение игрока — дрон стартует, когда игрок сместился на 12 px, а `startDelayMs` остался предохранителем для того, кто не двигается вообще. **SYSTEM CORE был непроходим по замеру:** орбитальный шип мёл x285-319 / y125-159 — коридор между ярусами 16 и 13 и обе посадки сразу (левый край яруса 16 смертелен 32 кадра из 120, правый край яруса 13 — 15, перелёт — 22), свободного момента на весь переход не существовало. Орбита перенесена на прыжок 19→16 с радиусом 1: все площадки 0/120, полёт 29/120. **Фальшивый выход заколочен.** Погасшего маяка (CLAUDE.md #4.7) владельцу не хватало третий раунд подряд; теперь проём перечёркнут двумя балками, а отказ при контакте — красная тряска вместо затухания, которое читалось как «дверь что-то делает». **Фантомные платформы неотличимы** по прямому требованию владельца, и единственное, что держит это честным, — тест: ни ямы, ни шипа, ни пистона под фантомом. Одного нарушителя он нашёл сразу — `fakep-01` в DISTRICT висел над ямой; перенесён на колонки 30-33, где падение стоит подъёма. |
| Падение шипа без паузы, трещина вместо заплаты | 2026-09-14 | 1.96 МБ | `phaser-*.js` — 1.41 МБ | **Шип снова падает сразу.** Предыдущая правка держала его неподвижным 250 мс, и владелец забраковал вид: «верни скорость падения шипа как до этого, сейчас он резко падает и трясётся сверху. Сделай чтобы сразу падал». Пауза и дрожание убраны; падение снова занимает весь `warningMs` на той же кривой `Cubic.easeIn`, а роль телеграфа взяла на себя сама кривая: кубика тратит первую четверть ВРЕМЕНИ на двадцатую часть ПУТИ, поэтому на 250-й миллисекунде из 560 шип сдвинулся примерно на 9 px — виден, очевидно падает и ещё никого не достал. Дальше смертелен до самого пола. Замер траектории: 115 → 116 → 121 → 128 → 147 → 167 → 215, из 15 кадров падения смертельны 14. Пункт 4.2 выполняется буквально как написан. **«Стало видно стыки земли где яма будет».** Проверено пиксельно по всем 30 уровням — и сначала неверно: замеры снимались без учёта того, что уровень центрируется на холсте, а декоративные стены забирают по 70 px, так что я семь колонок подряд мерил не то место. С поправкой земля оказалась однородной везде, включая люки (бирюзовая кромка идёт через них непрерывно), а настоящая причина нашлась выше: падающие камни, висящие над ямой, рисовались ТЕКСТУРОЙ ЗЕМЛИ посреди ряда настоящих плит. Это было сделано намеренно («visibly not one of the level's solid slabs»), и на нормальном расстоянии давало именно то, что владелец описал — ряд разномастных заплат. Теперь это тот же объект в другом состоянии: `tile-platform-slab-cracked` — та же плита, тот же контур, та же кромка, но с трещиной и притушенной кромкой. Признак остаётся читаемым намеренно: эти камни висят над ямами, а с падающего пола больше нельзя оттолкнуться (решение владельца, CLAUDE.md #4), так что трещина — единственное предупреждение, которое там есть. |
| Земля перестала выдавать ямы | 2026-09-14 | 1.96 МБ | `phaser-*.js` — 1.41 МБ | Владелец прислал кроп земли и показал стык. Нашлись две причины, обе — декор, говоривший на языке ловушек. **Первая: метка края ставилась у закрытой ямы.** Тёплая полоска `dangerAlt`, которой помечается обрыв, вешается на колонку, соседнюю с открытой дырой; список «чем дыра сейчас закрыта» собирался только по люкам, а уезжающая яма (`shiftingPit`, armed `moving-platform`) в него не попадала. В BEAM это давало метку на колонке 40 при плите на 37-39, в SHIFT — на 42 при плите на 39-41: уровень заранее помечал то место, где дыра появится после срабатывания. Теперь стартовые колонки такой плиты тоже считаются закрытыми, и метка остаётся только у настоящего обрыва (BEAM: было 15,20,33,40 → стало 15,20,33). **Вторая: декоративный «повреждённый» тайл.** Он выпадал на 5% колонок и рисовался тёмной выбоиной с тёплой полоской над ней — в том же `dangerAlt`, что и кромка ямы. То есть декор произносил ровно ту фразу, которой уровень предупреждает об обрыве. В DROP один такой стоял на колонке 38 при люке на 39-41 — от предупреждения не отличить. Вариант убран целиком; разнообразие поверхности несут швы панелей и лампы, которые про опасность не говорят ничего. **Замеры по дороге:** кромка проверена попиксельно на всех 30 уровнях и непрерывна, включая люки; сама земля над люками совпадает с соседней колонка в колонку. |
| Засечка на горизонте: щель между домами | 2026-09-14 | 1.96 МБ | `phaser-*.js` — 1.41 МБ | Тёмная засечка, которую владелец показал на кропе, пережила прошлую правку — и правильно, потому что искал я не там. Две предыдущие догадки (метка края у закрытой ямы, декоративная выбоина) были настоящими дефектами и стоили того, чтобы их убрать, но засечкой были не они. Сканирование полосы над линией земли по всем 30 уровням с выключенными игроком, дверью и ловушками показало, что отметины стоят НАД поверхностью, а не на ней, и не меняются при правках текстуры пола. Источник — **зазор между двумя силуэтами фона**: в DROP две башни почти смыкаются и оставляют щель шириной 3 px, идущую сверху донизу и упирающуюся в кромку. Ничего не значит, падает туда, куда её поставил seed, и читается игроком как метка. Добавлена сплошная дымка у горизонта — полоса цвета среднего слоя, затухающая вверх за семь шагов, — которая обрезает каждую такую щель до того, как та дойдёт до земли. После правки детектор даёт clean на всех 30 уровнях (было 24 уровня с отметинами). Заодно исправлены сами швы панелей: вариант с полосами по краям тайла давал на стыке двух таких тайлов двойную линию в 2 px (замер на DROP, x 198-199) — вдвое шире и темнее, чем шов должен быть. Полосы сдвинуты внутрь тайла, альфа 0.6 → 0.45. |
| Швы панелей убраны с пола | 2026-09-14 | 1.96 МБ | `phaser-*.js` — 1.41 МБ | Засечка пережила ещё одну правку, и по полноэкранному скриншоту владельца наконец стало видно, почему я её три раунда не находил: я мерил внутренний буфер 620×270, где всё было честно однородно, а он смотрит на растянутую картинку. Его окно — 1880×900, то есть масштаб 3.33 при фиксированной виртуальной высоте 270. Каждая однопиксельная линия шва панели на этом масштабе рисуется шириной 3-4 px. Замер на растянутом кадре BOOT: **двенадцать тёмных вертикальных штрихов** по всей длине земли, на нерегулярных интервалах. Владелец показал на тот, что ближе к люку, и был прав по сути: на полу, который в любой колонке может провалиться, тёмная черта — это метка. Швы убраны с поверхности полностью, вместе с такими же полосами по краям подповерхностной заливки (в tileSprite они повторялись каждые 10 px). Разнообразие поверхности теперь несут только лампы. После правки на том же растянутом кадре остался **один** штрих — тёмная кромка `tile-ground-edge` у настоящей ямы, то есть единственная вертикальная отметина на полу снова что-то значит. Детектор по всем 30 уровням: clean. Текстур земли стало 2 вместо 9. |
| Зелёные квадраты в CRUMBLE и чёрная кромка ямы | 2026-09-14 | 1.96 МБ | `phaser-*.js` — 1.41 МБ | **Регрессия, которую я внёс сам предыдущим коммитом.** Удаляя варианты швов, я вырезал вместе с ними строку, регистрировавшую псевдоним `'tile-ground'` рядом с `'tile-ground-top'`. От этого псевдонима зависели три ловушки — исчезающая платформа, падающий пол и электропол, — и в CRUMBLE осыпающиеся уступы превратились в зелёные квадраты Phaser'а, которыми он рисует отсутствующую текстуру. Зелёными они были во всех сборках, а typecheck, lint, 873 теста, прод-сборка и живой прогон всех 30 уровней были при этом зелёными в другом смысле: ключ текстуры — обычная строка, и о нём никто ничего не сказал. Псевдоним не возвращён: три ловушки переведены на настоящий ключ, чтобы дублирующего имени больше не существовало. Появился `tests/texture-keys.test.ts` — он собирает все строковые ключи текстур из `src/` и требует, чтобы каждый регистрировался в `SpriteFactory`; проверено, что он падает ровно на этой ошибке. В живой прогон по 30 уровням добавлена проверка на объекты с `__MISSING`. **Чёрная засечка на яме** — это оказался последний кусок ложной разметки, и он был в тайле края: под тёплой полоской сидел блок `outline` шириной 3 px во всю высоту тайла, то есть чёрная вертикальная черта на полу. Смысл всегда нёс тёплый маркер; блок лишь делал вид, что пол там треснул. Убран. Заодно тайл края разделён на левый и правый: раньше один и тот же тайл служил обеим сторонам ямы и всегда метил свою правую половину, так что у дальнего края метка стояла не с той стороны. Детектор на растянутом кадре (1880×900, масштаб 3.33) теперь не находит на земле ни одной вертикальной отметины. |
| Край силуэта под дверью, платформы из материала земли | 2026-09-14 | 1.96 МБ | `phaser-*.js` — 1.41 МБ | **Засечка под порталом — тот же горизонт, недоделанный до конца.** Дымка, положенная в прошлый раз, была полупрозрачной (alpha 1 → 0.16 за семь шагов), и на 0.5-0.8 край силуэта по-прежнему просвечивал через неё контрастной ступенькой, идущей до самой кромки. Уровень опознан по геометрии из скриншота владельца — шипы на 9-10, яма 17-21, банк на 24, дверь на 34 — это SYSTEM CORE, и детектор показывал отметину ровно на колонке 34.4, то есть под дверью. Первые 10 px дымки сделаны непрозрачными, затухание начинается выше: от y 210 до кромки фон теперь однороден по всей ширине, ASCII-дамп подтверждает. Детектор на растянутом кадре 1574×705 больше не находит SYSTEM CORE в списке. Что осталось в отчёте детектора и НЕ является дефектом: стойки самой двери (колонки 42.8-43.0 и 45.1-45.3 на уровнях с выходом на земле) и стойки фальшивой двери в MIRROR — это сами объекты, а не разметка на полу. **Платформы стали материалом земли** по прямой просьбе: верх плиты теперь побайтово тот же, что у `drawGroundTop` — тот же кап в 3 px, та же бирюзовая кромка в 2 px, то же тело. Единственное отличие, которое осталось, — тёмная линия снизу: у земли низа нет, а у плиты, висящей в воздухе, он должен быть. Трещина на падающей плите сдвинута на 1 px вниз вслед за новым капом. |
| Кромка пола переехала в тайл | 2026-09-14 | 1.96 МБ | `phaser-*.js` — 1.41 МБ | **Засечка справа от ямы наконец найдена, и она была моя собственная.** Владелец прислал третий кроп подряд с одной и той же тёмной чёрточкой на бирюзовой кромке. Первые два захода я мерил не то: `snapshotArea` смещает читаемую область по вертикали при разной высоте запроса, и полоса, которую я считал кромкой, ею не была — дамп тех же координат показывал идеально однородную линию. Замер переведён на прямое чтение холста (`drawImage` игрового canvas в свой и `getImageData`), и засечки проявились сразу: **ровно 1 px шириной, высотой ровно 2 px — то есть во всю толщину кромки и ни пикселем больше, — и всегда на целой границе колонки.** Список: BOOT col 37, DROP 15/25/35/42, SHIFT 13, ASCENT 15, BOOT COMPLETE 38, GRID CORE 15, BEAM 40, GHOST FLOOR 15. Каждая из них — стык земли с люком или с уезжающей плитой, то есть метка ровно там, где пол собирается исчезнуть. Причина: яркая кромка рисовалась НЕ в тайле, а поверх пола отдельными фигурами — один `Rectangle` на каждый сплошной участок земли (`Level.ts`), плюс по одному на каждый люк (`FallingPlatformTrap`) и на каждую скользящую плиту (`MovingPlatformTrap`). Там, где два таких прямоугольника встречались встык, общий пиксель не доставался ни одному: замер на BOOT, x=370 — 32,101,114 вместо 70,220,233, то есть чистая подложка тайла. Проверено по одному объекту: спрятать правый прямоугольник — пиксель не меняется, спрятать левый — не меняется, поднять правый по depth — пиксель становится нормальным. На экране владельца при масштабе ~2.6-3.3 этот 1 px рисуется шириной 3-4. Исправление: кромка нарисована внутрь самих текстур (`drawGroundTop` и `drawPlatformSlab`, теми же двумя проходами в том же порядке), а все три накладываемых прямоугольника удалены. Тайлы — текстурированные квады и стыкуются точно. Цвет кромки сохранён: было 70,220,233, стало 71,221,234. После правки скан всех 30 уровней не находит на кромке ни одной отметины в 2 px; остаётся только лип настоящей ямы во всю высоту тайла (SHIFT 16, GRID CORE 15, CHASE 16) и стойки фальшивой двери в MIRROR — объекты, а не разметка. Кромки всех летающих плит проверены отдельно и однородны. Телеграф падающей плиты не пострадал: её кромка 33,98,110 против 71,221,234 у целой. Закреплено тестом `tests/ground-lip-invariants.test.ts` — проверено, что он падает, если вернуть прямоугольник на место. |
| Засады срабатывали позади игрока; смерти сектора не считались | 2026-09-14 | 1.96 МБ | `phaser-*.js` — 1.41 МБ | **«Успеваю пробежать» — и это было буквально так.** Владелец назвал PATROL, GRID CORE, BEAM и «почти все уровни». Замер на бегущем игроке (подход от триггера — 318 мс при `moveSpeed` 110): банк шипов на PATROL пересекался на 298-й миллисекунде после срабатывания, а становился смертельным только на 567-й — 269 мс пустоты, шипы ещё под полом, когда игрок по ним пробежал. Падающий шип на GRID CORE был смертелен вовремя, на 267 мс, и всё равно бесполезен: на кривой `Cubic.easeIn` за 560 мс он к моменту прохода игрока прошёл 13% пути и висел в 92 px над его головой. Причина одна на оба случая и внесена мной же в прошлом раунде: `AMBUSH_DROP_TIMING` с `warningMs` 560 был введён для падающего шипа (250 мс телеграфа + полёт), но применён заодно и к банку шипов, которому висеть не нужно. Константа удалена целиком, оба хелпера вернулись на общий `AMBUSH_TIMING` (320 мс) — он и написан как расстояние подхода. Отдельно нашёлся BOOT: его засада была написана руками мимо хелпера, триггер шириной 2 колонки не доставал до шипа (подход 233 мс против окна 320) и был высотой 3 тайла вместо `APPROACH_BAND_TILES` — то есть прыгающий игрок пролетал над ним; переведён на `dropSpike`. Живой аудит всех засад, срабатывающих от подхода: **28 из 29 ловят бегущего** (до правки PATROL/GRID CORE/BEAM пропускали); 29-я — блокирующий шип на верхней площадке CRUMBLE, он по замыслу приземляется между игроком и дверью и ждёт, а не бьёт бегущего. Закреплено тестом: окно предупреждения засады не короче `MIN_WARNING_MS` и не длиннее подхода, вычисленного из ширины её собственного триггера. **Смерти на первом уровне каждого сектора не считались.** `GameplayScene.create` вызывал `startSector()` по условию «id заканчивается на `-level-01`», а `create()` выполняется заново при каждом рестарте после смерти — то есть каждая смерть на первом уровне сектора обнуляла счётчики этого сектора. Замер: на BOOT четыре смерти, `sector.deaths` так и остался 0, при этом `run.deaths` считал 1-2-3-4 верно. Причина смерти была ни при чём — дело было в том, на каком уровне играешь. Теперь сравнивается `sectorIdOf(levelId)` с `GameState.currentSectorId` (и значение по умолчанию у него больше не `'sector-01'`, иначе первый же сектор сессии не считался бы сменой). Проверено живьём: смерти копятся через рестарты, обнуляются ровно на границе сектора. **Падающие блоки стали неотличимы** по прямой просьбе владельца — трещина и притушенная кромка убраны, в воздухе блок берёт ту же текстуру плиты и тот же болт по тому же хешу колонки. Замер: 0 различающихся пикселей из 100 против настоящей плиты. `tile-platform-slab-cracked` удалён как мёртвый. Решение записано в CLAUDE.md #4 вместе с тем, что удерживает его честным: солвер по-прежнему не считает ни один падающий пол поверхностью. |
| Адаптивные варианты уровней удалены | 2026-09-14 | 1.96 МБ | `phaser-*.js` — 1.41 МБ | **Система `gentle`/`bold`/`troll` убрана целиком** по решению владельца: «давай уберём систему gentle bold troll, чтобы всегда у нас было одинаково». Удалены `data/levels/variants.ts` (одиннадцать перетюненных копий уровней) и `ai/DifficultyDirector.ts` вместе с их тестами; `getLevel()` больше не принимает вариант, `GameState.currentVariantId` и `GameplaySceneData.forceVariantId` исчезли, событие `level:loaded` больше не несёт вариант. Два места, где вариант был не украшением, а логикой: лидерборд отбрасывал всё, кроме канонического варианта (теперь фильтровать нечего — время на уровне есть время на уровне), и призраки хранились по ключу `levelId::variantId`, потому что геометрия вариантов различалась. Ключ стал просто `levelId`, а существующие сейвы мигрируются в `parseSave`: канонические записи переносятся вперёд, записи из `gentle`/`bold` отбрасываются — их геометрии больше нет, и призрак прошёл бы сквозь стену. Покрыто тремя тестами, включая случай, когда в сейве лежат обе формы ключа (побеждает быстрейшая). Версия сейва 5 → 6. THE SYSTEM продолжает наблюдать и комментировать — она перестала менять то, за чем наблюдает; CLAUDE.md #4.1 и #6 переписаны под это. **BOOT COMPLETE: люк переехал под портал.** Владелец: «дыру после движущегося шипа делаем чуть ближе, чтобы игрок сразу проваливался, если бежал напрямик, прямо под порталом». Люк стоял на 33-35 колонках… точнее, на 35-37 — на две колонки правее самой двери (выход на 34), поэтому очевидная линия «выбежал из-под патрульного шипа, дальше наверх» проходила мимо него. Перенесён на 33-35, ровно под дверь; триггер (`TRAPDOOR_LEAD` = 1) встал на колонку 32 — тот самый тайл, где патрульный шип разворачивается, так что пол открывает именно уход от шипа на бегу. Замер: три прогона «зажать вправо» — три провала в яму на x=360. Уровень остаётся проходимым по солверу (яма 3 тайла против дальности прыжка 57.9 px). |
| Испытание дня стало режимом | 2026-09-14 | 1.97 МБ | `phaser-*.js` — 1.41 МБ | До этого испытание дня запускало обычный уровень кампании: пройдя его, игрок попадал на экран сектора и шёл дальше по кампании, уровень помечался пройденным, а точка возобновления PLAY уезжала. Теперь у забега свои правила и свой конец. **Правила:** три жизни на один сквозной таймер — смерть тратит жизнь и перезапускает уровень, время продолжает идти; на нуле забег окончен. HUD в этом режиме показывает оставшиеся жизни вместо потраченных попыток. **Никакого следа в кампании:** ни `markCompleted`, ни `setLastLevelId`, ни перехода к следующему уровню. Замер вживую: сыграл кампанию до `sector-02-level-02`, прошёл испытание — `completedLevels` остался пуст, `lastLevelId` так и указывает на `sector-02-level-02`. **`DailyResultScene`:** время, смерти, лучшее за сегодня, таблица лидеров уровня, обратный отсчёт до следующего испытания. Лидерборд взят уровня, а не отдельный по датам — борды Яндекса заводятся руками в консоли и не создаются на каждый день, а испытание играется на том же уровне в той же форме, что и кампания, так что время сопоставимо. **Rewarded-продолжение:** одно в сутки, предлагается только когда забег кончился из-за жизней, списывается до показа рекламы и возвращается, если реклама не отработала — проверено вживую без SDK: `continuesUsed` вернулся в 0, на экране «реклама недоступна». CREDITS платятся за первое прохождение дня, иначе испытание превратилось бы в кран. Сейв: раздел `daily` (версия 6) держит только сегодняшний день. Проверено на 480 и 620 px — всё содержимое в пределах 480, ноль ошибок консоли. |
| Авто-деградация по FPS и dev-оверлей | 2026-09-14 | 1.97 МБ | `phaser-*.js` — 1.41 МБ | **Деградация из CLAUDE.md #9 была записана в правилах и не существовала в коде** — ни замера FPS, ни менеджера качества. Теперь есть `fx/PerformanceGuard.ts`: скользящее среднее по 30 кадрам (кадры длиннее 200 мс отбрасываются — это стойл, а не частота) и чистая функция решения с гистерезисом. Понижение при среднем ниже 50 FPS, держащемся 1.5 с; возврат — только выше 58 FPS в течение 6 с. Разрыв между порогами существует затем, чтобы устройство, стоящее ровно на границе, не мигало слоями каждую секунду. Порядок ровно тот, что в правиле: частицы (включая косметический трейл), затем фоновые слои, затем экранные эффекты. Выбор игрока и решение игры разведены: `FxSettings` — то, что выключил игрок, `FxQuality` — то, что сняла нагрузка; деградация никогда не переписывает настройку, иначе включение частиц обратно молча не сработало бы. Ничего из того, что игрок обязан увидеть, в списке нет — фаза предупреждения ловушки ставит себе alpha сама, а не через `FxManager` (см. `AmbushSpikeTrap`), так что на любом уровне деградации телеграф остаётся (CLAUDE.md #4.2). **Проверено вживую троттлингом CPU в 20 раз:** тир 0 → 1 через 1.5 с, → 2 через 3 с (слои `mid` и `signals` реально скрылись), → 3 через 4.5 с; после снятия троттлинга тир пошёл обратно через ~8 с. Ноль ошибок. Логика покрыта 13 тестами (пороги, гистерезис, порядок, невмешательство в настройки игрока). **Dev-оверлей** (`src/dev/DebugOverlay.ts`, F3): FPS, текущий тир, что именно сейчас включено, счётчики смертей и хитбоксы — тело игрока, hurt-бокс, опасные тела, триггеры и зоны выхода. Он нужен именно для того, чем кодом не закроешь: на настоящем телефоне частоту кадров видно только на экране. Загружается динамическим импортом под `import.meta.env.DEV`, как `ShopDevTools`. Первая версия собирала геометрию в самой сцене — и эти циклы остались в прод-бандле мёртвым кодом; сборка перенесена внутрь dev-модуля, в сцене остался один вызов, и `grep DebugOverlay dist/` теперь пуст. |
| Сценарии SDK: нет / медленный / отказ авторизации / лидерборд недоступен | 2026-09-14 | 1.97 МБ | `phaser-*.js` — 1.41 МБ | Размер не изменился — правка целиком в логике фасада. **Найден реальный дефект медленного SDK:** у тега `<script>` нет собственного таймаута — `onerror` срабатывает на отказ соединения и никогда на соединение, которое просто не отвечает, поэтому на телефоне, теряющем сеть посреди загрузки, `init()` оставался pending навсегда, а вместе с ним никогда не выполнялись облачная синхронизация и восстановление покупок, подвешенные на него цепочкой. Теперь `init()` гарантированно завершается за `SDK_INIT_TIMEOUT_MS` (8 с), но ожидание не бросается: пришедший позже SDK всё равно подхватывается — `LoadingAPI.ready()` и подписчики `onReady()` срабатывают поздно, а не никогда. Всё, что имеет смысл только с живым SDK, перенесено с цепочки за `init()` на `onReady()`; `SaveService.syncWithCloud()` больше не считает попытку без SDK за «синхронизация сделана». **Второй дефект — честность экрана результата:** `getLeaderboardEntries()` возвращал `[]` и когда таблица пуста, и когда её не удалось прочитать, из-за чего экран сектора показывал «—» упавшей таблице, а экран испытания дня — «лидерборд недоступен» пустой. Теперь `null` ≠ `[]`, добавлен ключ `resultLeaderboardEmpty`, и ссылка «Войти» на пустой таблице встала под сообщение, а не над ним. **Проверено вживую** (игра в реальном iframe с подставным `YaGames`): без SDK — скрипт даже не запрашивается, чтение таблицы `null`, игра играбельна, ноль ошибок; медленный SDK (ответ через 12 с) — в 3.5 с недоступен, затем `ready@12364` и облачная запись прошли; отказ авторизации — `granted=false`, ни одного `setScore`/`setData`, гость по-прежнему читает таблицу; таблица недоступна — все чтения `null`, на экране «ЛИДЕРБОРД НЕДОСТУПЕН», ноль ошибок. 21 новый тест (`tests/sdk-scenarios.test.ts`) гоняет фасад через его настоящий путь загрузки — подставные `window`/`document` и тег скрипта, которому тест сам решает, отвечать ли. |
| Мобильная проверка: вьюпорты, мультитач, ресайз | 2026-09-14 | 1.97 МБ | `phaser-*.js` — 1.41 МБ | Размер не изменился. **Худшая находка раунда — утечка подписки на `resize`:** `TouchControls` подписывался на ScaleManager (общий для всей игры, переживает любую сцену) и не отписывался, поэтому каждая смерть оставляла ещё один живой обработчик с зонами уничтоженной сцены. Замерено до фикса: четыре изменения размера после нескольких смертей — четыре `TypeError` из `refreshHitArea`. На телефоне изменение вьюпорта — рядовое событие: уезжающая адресная строка, поворот, клавиатура. После фикса ноль ошибок; закреплено `tests/scale-subscription-teardown.test.ts`. **Второй дефект:** `round(width * scale)` давал канвас на пиксель шире вьюпорта (813 в 812 на 19.5:9, 893 в 892 на 20:9) — правая колонка уровня уезжала за край; теперь floor с потолком по вьюпорту. Девять вьюпортов от 480×270 до 1920×1080 прогнаны вживую, замеры сведены в таблицу `docs/qa-checklist.md` §2. Полосы сверху/снизу на экранах уже 16:9 (192 px на 1024×768) штатны и обоснованы в докстринге `ScaleController`. |
| Аудит бандла и упаковка для Яндекса | 2026-09-14 | **1.80 МБ** (было 1.97) | `phaser-*.js` — 1.41 МБ | **−180 КБ, и это не «ужали», а выбросили то, что никто не скачивал.** Fontsource отдаёт каждую гарнитуру дважды: `.woff2` первым в списке `src` и `.woff` за ним как фолбэк. WOFF2 поддерживается с Chrome 36, Safari 10, Firefox 39 и Edge 14 — то есть в любом браузере, где вообще запускаются Яндекс Игры, поэтому `.woff`-копии не запрашивал никто, но каждый килобайт из них всё равно ехал в архиве. Плагин `dropLegacyWoff` в `vite.config.ts` убирает их из сборки и вырезает соответствующий `url(...)` из CSS. Проверено на прод-сборке вживую: 12 запросов, **ноль** `.woff`, 8 `.woff2`, ноль 404, ноль внешних запросов, ноль ошибок консоли, кириллица обеих гарнитур рисуется. Свой код — ноль `console.log`, `DebugOverlay` и `ShopDevTools` в бандле отсутствуют. Добавлена команда `pnpm package`: проверяет сборку перед упаковкой (index.html в корне, ни одного запрещённого формата, ни одного отладочного следа, ни одного абсолютного пути — он сломался бы в iframe Яндекса) и только потом пакует. Готовый архив — **545 КБ**, 14 файлов, при лимите Яндекса 100 МБ. |
| Раунд правок по 12 замечаниям владельца | 2026-09-14 | 1.80 МБ | `phaser-*.js` — 1.41 МБ | Размер не изменился. Два бага ломали игру, а не только раздражали. **Первый — магазин.** Быстрое переключение вкладок роняло рендер: превью эффекта смерти клипится геометрической маской, её `Graphics` уничтожался вместе с вкладкой, а вспышки и полосы глитча живут ещё 140-260 мс на твине — и следующий кадр падал внутри рендерера (`Cannot read properties of null (reading 'renderWebGL')`). Канвас замирал на последнем кадре, а DOM-слой текста продолжал жить и рисовал новую вкладку поверх замёрзшей картинки — ровно то, что владелец описал как «зависает вкладка и ломается вся игра... текст и картинки зависают друг на друге». Воспроизведено 200 кликами по рейлу: одна ошибка до фикса, ноль после. **Второй — DOM-слой воскрешал скрытые элементы**: `applyStyle` переписывал `display` с нуля, поэтому спрятанный элемент возвращался от любого перекрашивания или ресайза; на экране «ВСЁ ПРОЙДЕНО» наведение мыши возвращало стрелку «дальше» поверх иконки меню. Плюс: удалена система призраков (осталось только личное лучшее время), убрана переменная высота прыжка, и в `SaveService` найден дефект миграции, стиравший кредиты и купленные облики при каждой смене версии сейва. |
| Прыжок обратно переменный; невидимый триггер на PATROL | 2026-09-14 | 1.80 МБ | `phaser-*.js` — 1.41 МБ | Размер не изменился — чистая логика. **Переменная высота прыжка вернулась одним сообщением позже, чем была убрана** («верни всё же разные прыжки, по степени нажатия пробела») — `jumpCutMultiplier: 0.45` и once-per-jump guard в `Player.ts` восстановлены дословно из истории git, не переизобретены. **Найден и обобщён реальный класс бага, не только случай PATROL:** `approach()` (`ambush.ts`) всегда считало высоту триггера от ОДНОЙ поверхности — той, вдоль которой игрок якобы бежит, — а не от любой платформы поблизости, с которой можно прыгнуть выше. На PATROL прыжок с уступа (`row 19`) поднимается на пик на три ряда выше прыжка с земли, для которого была посчитана высота триггера второго шипа (`dspike-01`) — и игрок в полёте оказывался ВЫШЕ зоны триггера на всём пролёте её колонок, попадая обратно в неё только у самого шипа: «могу перепрыгнуть триггер второго шипа и он активизируется чуть позже» (владелец). **Замерено вживую** покадровым опросом фаз ловушки: до фикса `dspike-01` весь пролёт оставался в `idle` (триггер не срабатывал вовсе), после — уходит в `warning` за ~130 мс после старта прыжка, за 330+ мс до подлёта к шипу, как и на обычном наземном подходе. Фикс не точечный: новая функция `widenForPlatforms` применяется автоматически ко всем триггерам всех уровней в `Level.ts` (по фактическому списку платформ уровня), и это вскрыло тот же недосмотр ещё в четырёх уровнях (`sector-04-level-05/06`, `sector-05-level-03/05`) — там триггер проходил впритык, на грани кадра, а не мимо; починены той же правкой, без единого числа, подобранного на глаз. Регрессия закреплена в `tests/ambush-widen.test.ts` с точными числами PATROL. 947 тестов, 30/30 проходимы. |

| Семь замечаний с плейтеста: пики вниз, PISTON ROW, CURRENT, GHOST FLOOR, DISTRICT, преследователи сектора 5, SYSTEM CORE | 2026-09-18 | 1.80 МБ | `phaser-*.js` — 1.41 МБ | Размер не изменился — чистая логика и данные уровней. **Текстура шипов теперь переворачивается по направлению угрозы**: `AmbushSpikeTrap` (всё, что падает сверху) и потолочный `spike-bank` (`yHidden < yLethal`) получили `setFlipY(true)` при создании — раньше единственная текстура «шип» рисовалась остриём вверх независимо от того, бьёт ли он из пола или падает сверху. **PISTON ROW**: `warningMs` 500→380 — это длительность самого твина выталкивания (`SpikeBankTrap`), так что короче значит резче, а не короче предупреждение (запас над `MIN_WARNING_MS`=250 сохранён). **CURRENT**: `idleMs` электро-пола 2400→1200 — при 2400 добрая половина пересечений (6 тайлов, ~545 мс на `moveSpeed`) укладывалась в один только «невидимый» отрезок цикла целиком, ловушка ни разу не доходила до `warning`: «сейчас ловушки на полу срабатывают с задержкой... успеваю полностью пробежать» (владелец). **Преследователи сектора 5** (`pursuer`): `speedFactor` уменьшен на всех трёх — 0.8→0.75 (HUNTED), 0.7→0.65×2 (CHASE, PRESSURE). **SYSTEM CORE**: тир на row 7 был в упор над `dp-01` и тиром на row 13 — три платформы в одну колонку с шагом ровно в высоту прыжка, что и открывало обход «нажать 3 раза пробел»; тир сдвинут на колонки тира row 10, ровно под который его уже держал маятник `swing-01` по собственному описанию. **GHOST FLOOR и DISTRICT потребовали по два прохода.** Первая версия GHOST FLOOR подвинула фальшивую лестницу под настоящий подъём, но рядами через одну строку от настоящих тиров — визуально монолитный столб без просвета: «между платформами нет расстояния, они буквально друг на друге стоят» (владелец). Исправлено смещением в сторону на 2-3 колонки от того тира, с которым совпадает высота — тот же приём, что уже работал у `fakep-01`/`fakep-03`. Первая версия DISTRICT сузила только верхний тир лестницы, но нижний по-прежнему доставал до его безопасной половины одним прыжком без сноса — «всё также легко проходится, с правой стороны платформы друг над другом без ловушки». Исправлено сдвигом НИЖНЕГО тира (34→30), так что тиры вообще не делят ни одной колонки — пересечь зазор без промаха в колонки орбитального шипа стало физически невозможно. 947 тестов, 30/30 уровней проходимы. |

| Фальшивый выход стал ловушкой | 2026-09-18 | 1.80 МБ | `phaser-*.js` — 1.41 МБ | Размер не изменился. Единственный в игре fake exit (`MIRROR`) два раунда подряд только отказывал — тряс дверь и мигал красным, — и оба раза вердикт владельца был один: сначала «вообще не понятно зачем нужен перечёркнутый фиолетовый портал, у него буквально нет никаких функций», потом «этот фиолетовый портал бесполезен, либо полностью переделываем, либо убираем его». Из двух вариантов выбрана переделка. **Дверь теперь забирает вошедшего** и возвращает его на точку старта уровня, таймер забега не останавливается: цена — весь путь обратно (на `MIRROR` двадцать пять колонок, пара шипов и поршень), и только он. Ни жизни, ни смерти в счётчике, ни конца забега — CLAUDE.md #4.7 («первое появление не смертельно») соблюдён с запасом: дверь не смертельна никогда, а точка возврата по построению безопасна, поэтому и #4.4 не нарушен. Транзит проходит в новом состоянии игрока `swallowed` (не `alive`), и это не косметика: все пофреймовые свипы сцены отсечены по `isAlive()`, так что летящего через уровень игрока нельзя ни убить, ни заставить сработать чужой триггер по пути. Буфер прыжка и coyote-таймер сбрасываются на выходе — иначе нажатие, сделанное за мгновение до закрытия двери, выстрелило бы уже на другом конце уровня. **Читаемость — половина правки:** молчаливый телепорт читался бы как баг, поэтому дверь вспыхивает фиолетовым и захлопывается, андроида видимо втягивает внутрь (220 мс), экран встряхивается на выходе, и SYSTEM тут же называет причину — новый пул реплик `data/dialogues/fakeExit.ts` со своей мешалкой, вне каскада смертей `Commentator` (никто не умер, ни одна категория смерти не подходит). **Проверено вживую** настоящим вводом с клавиатуры: игрок входит в дверь на 27-й колонке, через ~250 мс оказывается на спавне (col 2.59) живым, `scale`/`alpha` восстановлены, счётчик смертей 0, таймер идёт; управление возвращается полностью — удержание вправо 400 мс даёт ровно 4.44 колонки (110 px/с), прыжок работает; повторный заход в дверь срабатывает так же. Ноль ошибок консоли. 950 тестов (+3 на новый пул реплик), 30/30 уровней проходимы. |

| Обманная дверь неотличима и перестала быть неизбежной | 2026-09-18 | 1.80 МБ | `phaser-*.js` — 1.41 МБ | Размер не изменился. **Найден настоящий дефект, а не только косметика:** на `MIRROR` дверь стояла на колонке 27 — поперёк единственного наземного коридора. Подъём начинается на колонках 34-38, поэтому любой маршрут к настоящему выходу проходил через зону обманки: игрок не выбирал её, его через неё пропускали («портал невозможно обойти», владелец). Ловушка, от которой нельзя отказаться, — шлагбаум, и это вдвойне неверно для той, что стоит весь путь обратно. Дверь перенесена на колонку 43 — туда, где выход стоял все четыре предыдущих сектора: свернуть на подъём раньше ничего не стоит, а дойти до двери можно, только пройдя мимо собственного маршрута. **И она перестала отличаться от настоящей** («сделай тогда чтобы он визуально не отличался от обычного портала») — то же решение, что уже принято по `fake-platform` и `falling-platform`. Отличий было три, не одно: текстура (`exit-inactive` против `exit-active`), отсутствие пульсирующего глоу и посадка — обманка рисовалась с центральной привязкой и была утоплена в пол на 10 px. Теперь `FakeExit` собирается из рецепта настоящей двери (та же текстура, привязка к линии поверхности, тот же глоу 1400 мс), а не из параллельного списка чисел, который надо держать в синхроне руками. **Замерено вживую:** обе двери в одной сцене отдают идентичные `texture`/`origin`/`displayWidth`/`displayHeight`/`tint`; маршрут наверх проходится, ни разу не задев дверь; вход в дверь по-прежнему возвращает на спавн живым, ноль ошибок консоли. Порядок честности: нечитаемая И неизбежная цена была бы нечестной, нечитаемая и избегаемая — это и есть вопрос, в честь которого уровень назван. Записано в CLAUDE.md #4.7 вместе с новым требованием: обманная дверь обязана стоять в стороне от обязательного маршрута. 950 тестов, 30/30 проходимы. |

| Карта паузы показывала не то время | 2026-09-18 | 1.80 МБ | `phaser-*.js` — 1.41 МБ | Размер не изменился. Владелец: «я только начал сектор 5, а у меня уже счётчик времени 1 минута… логичнее чтобы после каждого сектора он сбрасывался». Первая догадка — что время копится по секторам — не подтвердилась: прогон настоящего пути (сектор 4 → победа → экран сектора → сектор 5) показал, что все четыре часов сбрасываются правильно, HUD выдал `0:02`, секторные тоже с нуля. Спросил, на каком экране он это видел; ответ — на паузе, и там дефект действительно был. Карта печатала `GameState.elapsedMs()` под подписью «Время попытки», а это другая величина: время на УРОВНЕ, которое намеренно переживает перезапуск после смерти, потому что на нём построены забег Испытания дня и время прохождения уровня. **Замерено:** семь смертей на одном уровне — HUD показывает `0:01` (1383 мс текущей попытки), а карта в двух сантиметрах от него напечатала бы `00:19.7`. Один экран, два разных числа, и большее — с неверной подписью; владелец разумно прочитал излишек как время, перенесённое из прошлых секторов. Карта теперь получает снимок часов текущей попытки из `GameplayScene.pauseGame` — снимок, а не живое чтение, потому что на время паузы часы и так заморожены. `GameState.run` не тронут: на нём висят лидерборд и дневное испытание. **Попутно найден, но не тронут, дефект того же класса:** секторные часы продолжают идти, пока игрок сидит в меню или магазине между уровнями, так что время сектора и его результат в лидерборде включают время, в которое никто не играл. |
| Часы перестали считать меню и магазин | 2026-09-18 | 1.80 МБ | `phaser-*.js` — 1.41 МБ | Размер не изменился — восемь строк логики. Владелец на найденный прошлой строкой дефект: «время в котором я сижу в меню и паузе не нужно учитывать». Пауза вычиталась уже давно (`GameState.pauseClock`/`resumeClock` из `PauseScene`), а промежуток МЕЖДУ уровнями — нет, и заметен он именно на секторных часах: они живут через несколько уровней, поэтому меню, магазин и карта «Сектор пройден» между ними записывались в результат сектора. Часы — настенные (`performance.now()`), а не фазеровские, ровно потому, что обязаны пережить `scene.start` с уровня на уровень, — значит остановка сцены их сама по себе не трогает. Починено симметрично той паре, которую CLAUDE.md #8 и так требует держать точной: `pauseClock()` встал рядом с `notifyGameplayStop()` в SHUTDOWN сцены, `resumeClock()` — рядом с `notifyGameplayStart()` в `create()`. Одно определение «идёт геймплей» на оба потребителя, а не второе рядом. Порядок внутри `create()` безопасен и это не совпадение: пока часы держат, `nowMs()` заморожен на моменте остановки, поэтому `startRun()`/`startSector()`, вызванные выше по функции, стартуют с той же точки, которую следующий `resumeClock()` и компенсирует, — новый уровень начинается ровно с нуля. **Замерено вживую:** 3 с на уровне 1 → 6 с в меню → уровень 2. Секторные часы: 7159 → 7174 (за шесть секунд меню набежало 15 мс перехода) → 9232 после двух секунд игры, то есть 7174 + 2058. Часы забега при этом честно обнулились на новом уровне (2058). Пауза не сломана: на карте паузы 5 с дали +21 мс, после «Продолжить» часы снова идут 1010 мс в секунду. Ноль ошибок консоли. 951 тест (+1 на промежуток между уровнями одного сектора), 30/30 проходимы. |
| Заголовок карты уровней наезжал на полосу прогресса | 2026-09-18 | 1.81 МБ | `phaser-*.js` — 1.41 МБ | +0.01 МБ — новый чистый модуль раскладки и один метод замера. Владелец прислал скриншот с наездом текста на полосу. **Замерено до правки** (480 px): заголовок «СЕКТОР 01 · SYSTEM BOOT» занимал `150 → 311`, полоса начиналась на `276` — наезд 35 px, и не на одном секторе, а **на всех пяти**; на `DATA DISTRICT` он просто заметнее, потому что название длиннее. Причин две, и обе настоящие. Первая: заголовок рисовался фиксированным 10 px кеглем и ни подо что не подгонялся, хотя все прочие экраны игры кегль под свою коробку подбирают. Вторая, и она важнее: полоса стояла на `min(sysX - 100, 352)`, то есть на 276 при ширине 480 — на 88 px левее, чем строка может себе позволить. Ограничение `sysX - 100` берегло полосу от колонки SYSTEM, но **та колонка начинается на `y = 38`, а эта строка живёт на `y = 11..19`** — рядом с полосой никогда ничего и не было. Теперь блок прогресса прижат вправо (ширина канваса минус поле, счётчик, зазор и полоса), а заголовок получает остаток и подгоняется под него новым `DomTextOverlay.lineFitSize`. Широкие экраны не тронуты: на 550 и 620 по-прежнему выигрывает 352, позиция из макета. Появился и `DomTextOverlay.measureWidth` — честный способ спросить ширину подписи, которой ещё нет: сосед `wordFitSize` меряет самое длинное **слово** (это нужно переносимому блоку), а однострочной подписи важна ширина всей строки. Геометрия вынесена в `config/sectorHeaderLayout.ts` и закрыта тестом на всех 141 ширине диапазона 480–620: в сцене её без браузера не проверить, а типографику наоборот — она проверена живьём. **Замерено после:** 15 сочетаний (три ширины × пять секторов) — минимальный зазор 13 px, кегль везде полные 10 px, ноль ошибок консоли. 956 тестов (+5), 30/30 проходимы. |
| Звёзды за уровень: правила, сейв, начисление, карта уровней | 2026-09-19 | 1.81 МБ | `phaser-*.js` — 1.41 МБ | Размер не изменился относительно предыдущей строки — чистая логика плюс пиксельная звезда из `fillRect`, ни одного ассета (#3). Владелец: «сейчас 5 этапов проходишь быстренько и всё». Разбор показал, что дело не в числе уровней: переигрывать уровни **уже** выгодно (`shop/EconomyRewards.ts` платит за каждое прохождение, включая повторное), рекорды времени пишутся и синхронизируются с облаком — но игра ни разу не **просила** переиграть, потому что рекорд не с чем сравнить. ★ за прохождение, ★★ без единой смерти, ★★★ без смертей и в пределах целевого времени. **Целевое время выведено, а не вписано руками.** Тридцать чисел (а скоро шестьдесят) никто не защитит, и первое же неверное делает третью звезду недостижимой на нормальном уровне. Поэтому солвер (`LevelValidator`) теперь умеет возвращать не только вердикт, но и сам путь — тот же BFS с запоминанием предшественника, — а `gameplay/parTime.ts` проходит по этому пути и считает время физикой из `jumpPhysics.ts` (туда добавлены `jumpAirTimeSec`/`fallTimeSec`: та же двухфазная гравитация, что и в `maxHorizontalReach`, выраженная временем, — разводить эту модель по двум файлам нельзя). Если маршрут изменится, цель поедет за ним, а не протухнет. **Честная граница названа явно:** `floorMs` — настоящий пол, его не побить, а всё сверху это допуск на то, чего геометрия не видит. Первая версия множителя была неверной и это видно на числах: множитель применялся к сумме «пол + ловушки», и уровень без единой таймингованной ловушки (DROP, GHOST FLOOR) получал 1.27 своего пола, а насыщенный ловушками — 2.4; меньше всего запаса доставалось тем, у кого его и так не было. Теперь множитель 1.8 умножает только пол, а ожидание ловушек добавляется отдельно. Разброс по кампании: 6.7 с (DROP) … 13.4 с (SYSTEM CORE). Поле `LevelDef.starTimeMs` существует, чтобы поправить расчёт руками после живой игры; тест не даёт поставить цель ниже пола. **Сейв v7 → v8**, `levelStars`. Миграция уже была полевой, поэтому старый сейв просто приходит без звёзд — и получает **обратную засыпку по ★ за каждый пройденный уровень**: это единственное, что старый сейв способен доказать, а показать прошедшему кампанию 0 из 90 значило бы показать потерю прогресса (#8). Звёзды хранятся, а не выводятся из `levelBests`: рекорд времени пишется и после прохождения со смертями, так что быстрый-но-смертельный заход плюс медленный чистый сложились бы в третью звезду, которой не заслужил ни один из них. Слияние с облаком зеркально времени: там побеждает меньшее число, здесь большее. **Замерено вживую** четырьмя настоящими прохождениями BOOT (цель 7594 мс): 4 смерти/30 с → ★, +25 кредитов; 0 смертей/30 с → ★★, +35; 0 смертей/5 с → ★★★, +35; **повтор того же трёхзвёздного захода → +20, доплаты за звёзды нет** — это и есть защита от кредитного крана. Ноль ошибок консоли. На карте уровней звёзды заняли ровно тот средний ряд, который макет всегда рисовал (номер, чипы, время) и который пустовал: комментарий в коде так и говорил — «дыра 40 px в каждой плитке». 1005 тестов (+49), 30/30 проходимы. |

| Звёзды на экране сектора, облик за звёзды, цель в `pnpm levels` | 2026-09-19 | 1.81 МБ | `phaser-*.js` — 1.41 МБ | Размер не изменился. Три вещи, замыкающие звёзды. **Экран сектора** получил счёт звёзд в той единственной полосе 30 px, которую раскладка оставляла свободной — сразу после двух чисел, которые решают две из трёх звёзд; считает весь сектор, а не забег — игроку нужно знать, сколько ещё стоит вернуться. **Облик `reference` («ЭТАЛОН»)** — единственный предмет, который нельзя купить, только заработать; это ответ на реальную арифметическую проблему — звёзды платят кредитами, и каталог, где всё покупаемо, просто получил бы больше денег на те же вещи. Порог — половина всех звёзд кампании, выведенная из `SECTOR_COUNT`, а не вписанная числом. Цвет визора — хью ~84°: широчайший оставшийся зазор (34° от `reward`, 53° от `patrolVisor`); второй широкий зазор ~350° отвергнут нарочно — он рядом с красным, который `drawPlayerFrame` насильно ставит на урон и смерть, а облик, который в норме выглядит раненым, притупил бы этот телеграф. **Три захардкоженные пятёрки убраны**: «5 СЕКТОРОВ» распалось на единицу измерения плюс число из `SECTOR_COUNT`, описание «ЯДРА» больше не называет номер сектора вовсе: число, вписанное в перевод, протухает молча. **`pnpm levels` теперь печатает цель и пол** рядом с вердиктом проходимости — это было сначала обещано в `docs/level-editing.md` §4.1, и обещание закрыто кодом, а не оставлено в тексте. **Замерено вживую:** 38 звёзд — карточка «???» с подписью «45 ЗВЁЗД», облика в инвентаре нет; 60 звёзд — «ЭТАЛОН · ЕСТЬ», выдан автоматически, чип в «СОБРАНО ОБЛИКОВ» на месте. Седьмой облик сетку не сломал: она уже умела расти третьим рядом и сама ужала карточки с 92 до 67 px. Экран сектора проверен на 480 и 620 px. Ноль ошибок консоли. 1008 тестов (+3), 30/30 проходимы. |

| Катапульта (`launch-pad`) — механика сектора 06 | 2026-09-19 | 1.82 МБ | `phaser-*.js` — 1.41 МБ | +0.01 МБ — класс ловушки и три функции физики, ни одного ассета. Первый объект в игре, который не мешает, а несёт: твёрдая поверхность, которая раз в цикл подбрасывает стоящего выше любого прыжка. Никогда не смертельна, но телеграфирует как любая таймингованная ловушка — подбросить без предупреждения в шип значило бы убить без возможности среагировать. Солвер знает про подброс — иначе уровень, верхняя половина которого нарочно вне досягаемости прыжка, был бы объявлен непроходимым. **Живые замеры вскрыли два настоящих дефекта, которых не видно ни в коде, ни в тестах.** Первый: `Player` ограничивает тело по `maxFallSpeed` в **обе** стороны, и Arcade молча срезал импульс — катапульта на 12 тайлов поднимала на 10.1 при скорости ровно −420. Потолок — 9.8 тайла, теперь выведен из физики (`MAX_LAUNCH_LIFT_PX`), а не вписан. Второй: даже в разрешённых пределах подъём на треть тайла меньше номинала (замерено: 8.70 при обещанных 9) — за первый кадр гравитация успевает отнять своё, чего непрерывная дуга не платит. Оба дефекта одного класса и самого опасного: солвер сертифицировал бы уровень, который игрок не пройдёт. Теперь все расчёты идут через `usableLiftPx`, и он нарочно занижен: вычитается целый кадр, а не половина, которую даёт арифметика — «иногда не допрыгивает» хуже, чем «никогда». Третье место, где могло молча сломаться: срез прыжка (`jumpCutMultiplier`) урезал бы подброс до 45% уже на следующем кадре, ведь игрок кнопку не держит; `Player.launch` ставит `jumpCutApplied`, и это же честное правило со стороны игрока: подъём, который ты не выбирал, ты и не обрезаешь. Катапульта с `loop: false` в солвере не считается способом подняться (выстрела может не случиться), но остаётся поверхностью. **Сквозная проверка:** уступ на 8 тайлов (прыжок достаёт 3.47) — солвер говорит `valid`, игрок реально встаёт на него (y=140 ровно по платформе, скорость 0), ноль ошибок консоли. 1018 тестов (+10), 30/30 проходимы. Ни один уровень катапульту пока не использует — сектор 06 следующим раундом. |
| Сектор 06 OVERCLOCK — шесть уровней на катапульте | 2026-09-19 | 1.82 МБ | `phaser-*.js` — 1.41 МБ | Размер не изменился: шесть уровней — это данные, не ассеты. Первый сектор за пределами исходной кампании и первый, построенный на объекте, который помогает. Вопрос сектора — «то, что тебя убивало, теперь тебя несёт»: LIFT-OFF (верхняя половина экрана просто вне прыжка), WINDOW (катапульта ставит тебя куда надо, лучи решают когда), CHAIN (катапульта забрасывает на катапульту), CROSSING (тот же бросок вбок: время полёта как расстояние), HOLD (ожидание, за которое платит преследователь из сектора 05), OVERCLOCK (от пола до потолка, три запуска). Каждый ярус, куда доставляет катапульта, стоит на 4-5 тайлов выше точки старта при прыжке в 3.47 — **нарочно**: если бы туда можно было залезть и так, катапульта была бы игрушкой, а уровни — про что-то другое. Все шесть проходимы солвером, который знает про подброс. **Живая проверка вскрыла три вещи, которых не видно ни в коде, ни в тестах.** (1) На CROSSING солвер подписал перелёт через яму — 70 px разрыва против 79 px охвата, — и игра показала, чего стоят эти девять пикселей: запуск не с крайней плитки катапульты, а с середины, и андроид в яме. Запас меньше, чем «где именно игрок стоял», — это не переправа, а монетка со справкой; добавлен уступ над дальней половиной ямы (50 px против 68), подброс поднят до 8 тайлов, прыжком туда по-прежнему не попасть (40.6 px). (2) На OVERCLOCK две катапульты стояли строго одна над другой: первый запуск пролетал ярус насквозь, приземлялся на вторую и швырял снова — два запуска на одно решение, и ярус между ними был декорацией. Разнесены на четыре колонки. (3) Последний запуск сектора бил строго вверх; крыша сдвинута влево, чтобы единственный раз за сектор пришлось целиться. Девять запусков по шести уровням замерены поимённо: каждый кладёт игрока ровно на тот ряд, на который рассчитан, ноль ошибок консоли. **И отдельно — дыра в воротах, которую нашёл сам сектор.** Звёздный порог проверялся на всех плитках карты уровней, кроме одной — большой карточки «ДАЛЬШЕ», то есть первого непройденного уровня сектора; а на чужом секторе это ровно его первый уровень. Замерено: сейв без единого пройденного уровня и без звёзд запускал `sector-06-level-01` одним тапом. Вторая дверь была ещё шире: экран «СЕКТОР ПРОЙДЕН» вёл в следующий сектор кнопкой, вообще не спрашивая про звёзды, — а это основной путь игрока. Обе закрыты и проверены живьём: при 30 звёздах карточка заперта и кнопка говорит «СЕКТОР ЗАКРЫТ · НУЖНО ЗВЁЗД: 45», при 60 — сектор открывается и уровень стартует. **Тест уровней больше не перечисляет секторы руками**: он ходил по списку из пяти импортов, и сектор 06 прошёл бы мимо всех проверок честности незамеченным; теперь источник — `LevelFactory`, тот же реестр, из которого игра берёт уровни. Проверка «каждый ярус достижим цепочкой прыжков» научена катапульте — по `usableLiftPx`, а не по номиналу. Scope lock владельца переписан в `TODO.md` явно: 5 секторов → 10. 1147 тестов (+129, из них 126 — старые проверки, впервые применённые к новому сектору), 36/36 проходимы. |

Бюджет (3 МБ) проверяется автоматически: `pnpm size` завершается с кодом
выхода 1, если `dist/` превышает потолок.

## План по FPS-деградации (реализован, см. строку замера от 2026-09-14)

При падении FPS ниже целевого на замере — снижать в этом порядке:
1. количество частиц;
2. второстепенные фоновые слои (параллакс, digital rain и т.п.);
3. экранные эффекты (chromatic/glitch, screen shake).

Физика и ввод никогда не деградируют — см. `CLAUDE.md` #9.
